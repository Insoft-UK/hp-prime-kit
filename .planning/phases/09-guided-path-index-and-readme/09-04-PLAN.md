---
phase: 09-guided-path-index-and-readme
plan: 04
type: execute
depends_on: [09-03]
autonomous: false
requirements: [READ-05, READ-06]
files_modified:
  - README.md
  - AGENTS.md
  - SKILL.md
  - docs/ai/prompts.md
  - examples/apptest/README.md
  - tests/test_reference.py
must_haves:
  truths:
    - "The README presents the documentation first: what it is, how a claim is labelled, where a person starts and where a model starts; then the tools; then working with an AI"
    - "A test fails when a number the README states about the documentation stops matching it"
    - "AGENTS.md, SKILL.md and docs/ai/prompts.md send a model to docs/llms.txt first"
    - "Every rule in the paste block names the fact or entry it comes from, with its label, and a test holds each identifier to a fact that exists"
    - "Nothing above the documentation says what the reference contradicts"
    - "The user has read the README draft before it is committed"
---

<objective>
Decisions 7 and 8 of `09-CONTEXT.md`, READ-05 and READ-06: the pages above
the documentation lead to it, and say nothing it contradicts.
</objective>

<context>
@.planning/phases/09-guided-path-index-and-readme/09-CONTEXT.md (decisions 7 and 8)
@.planning/phases/09-guided-path-index-and-readme/09-03-SUMMARY.md (left for this plan)

Measured before planning, on 2026-09-24, from `docs.load()` and the list of
names:

| | |
|---|---|
| entries | 598 of the 706 names that get one |
| facts | 117: 76 `G2`, 19 `emulator`, 22 `unverified`; 5 of them refuted hypotheses, all in `ppl.md` |
| examples | 800: 779 with a stored answer, 4 measured on a G2 with none stored, 17 *no value* |
| by kind | statements 14/14, commands 98/98, functions 177/177, app functions 179/179, app variables 130/172, variables 0/65, `GET` 0/1 |

What the pages above say that the reference contradicts:

- `README.md`: 741 of the 800 examples run on the emulator, where it is 779;
  it leads with the tools and sends a model to `AGENTS.md`, not to the index.
- `AGENTS.md`: `L(0)` is a run-time error (not measured, `ppl.one-based`);
  four refuted hypotheses, where there are five.
- `SKILL.md`: "§2" of `ppl.md`, which has no numbered sections; four
  hypotheses.
- `docs/ai/prompts.md` §1: `L(0)`; lists passed by value, where the fact is
  about matrices; `TEXTOUT_P`'s width as a seventh argument, true only with a
  grob; no debugger, where `DEBUG` has an entry; `main.py` as required, where
  `apps.main-py` is `unverified`. Its task prompts repeat the seventh argument
  and say not to rely on `[Num]` and `[View]`, which arrive as keys.
- `examples/apptest/README.md`: asks for the six labels along the bottom to be
  pressed to settle whether 11 is `[Num]` or "soft key 6".
  `interface.soft-labels-not-keys` settled it: the labels report nothing, and
  what published apps call soft key 6 is the physical `[Num]`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: AGENTS.md and SKILL.md</name>
  <files>AGENTS.md, SKILL.md</files>
  <action>Both send a model to `docs/llms.txt` first, and to `ppl.md` for the
  limits and the five refuted hypotheses. The non-negotiables name their
  facts, and the index rule says what was measured. §3 of `AGENTS.md` also
  asks for a command to be cited by its entry.</action>
</task>

<task type="auto">
  <name>Task 2: The paste block</name>
  <files>docs/ai/prompts.md, tests/test_reference.py</files>
  <action>The header sends a model that can read files to `docs/llms.txt`.
  Every rule of §1 ends with the fact or the entry it comes from and its
  label, in the linter's shape, `[ppl.local-limit, G2]`; the five
  contradictions are corrected. It keeps restating, on purpose, and says the
  reference wins where they differ. The task prompts lose the seventh argument
  and the advice about `[Num]` and `[View]`. A test: every rule in §1 names
  an identifier, and every identifier is a fact or an entry that exists, with
  the label the fact carries.</action>
</task>

<task type="auto">
  <name>Task 3: The apptest README</name>
  <files>examples/apptest/README.md</files>
  <action>The open question goes: it is settled, with its link.</action>
</task>

<task type="checkpoint">
  <name>Task 4: The README, as a draft</name>
  <files>README.md, tests/test_reference.py</files>
  <action>Rewrite in the order of decision 7: what the documentation is and
  how a claim is labelled; where a person starts, the guided path; where a
  model starts, `docs/llms.txt`; the reference; then the tools; then working
  with an AI; then what it does not do and the status. Its numbers are the
  table above, written so a test can find them. The test computes them from
  the documentation and fails when the README states another. The draft goes
  to the user, and is committed only once they have read it.</action>
</task>

</tasks>

<verification>
The whole suite passes; the link check covers the new pages; the README is
committed with the user's approval; Phase 9's verification is written, with
its count to be taken again when Phase 8.1 closes.
</verification>
