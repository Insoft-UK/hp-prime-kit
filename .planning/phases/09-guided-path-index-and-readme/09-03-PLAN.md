---
phase: 09-guided-path-index-and-readme
plan: 03
type: execute
depends_on: [09-02]
autonomous: true
requirements: [READ-01]
files_modified:
  - hpkit/docs.py
  - hpkit/cli.py
  - hpkit/compare.py
  - templates/starters/program.txt
  - tests/test_reference.py
  - tests/test_examples.py
  - tests/test_cli.py
  - docs/start/01-setup.md to 06-working-with-ai.md
  - docs/topics/ppl.md
  - docs/tools.md
  - README.md (one command line)
must_haves:
  truths:
    - "The check reads docs/start/ like the rest of the documentation: links, facts named, the layer above, planning words"
    - "Every platform claim on the path links to the fact or the entry that holds it, and says no more than that fact"
    - "The three errors of 09-CONTEXT are gone, and so are the ones found in the reading below"
    - "Step 6 sends a model to docs/llms.txt, not to AGENTS.md, SKILL.md or docs/ai/"
    - "The starter exports CIRCAREA, a name that is not on HP's list"
    - "Every program on the path lints with no error and no warning, and a test holds it there"
---

<objective>
Decision 6 of `09-CONTEXT.md`, READ-01: the six steps of the guided path, in
their order, rewritten to link rather than restate, checked like the rest of
the documentation.
</objective>

<context>
@.planning/phases/09-guided-path-index-and-readme/09-CONTEXT.md (decision 6)
@hpkit/docs.py      _layer_files(), cross_check(), ABOVE, PLANNING
@docs/start/        the six steps
@docs/llms.txt      every fact and entry the path can link to

Measured before planning, on 2026-09-23, by reading every step against the
facts it relies on and running every command it gives in a scratch folder:

- `hpprime new`, `lint`, `run`, `write`, `read`, `build`, `verify` and
  `new --python` all do what the path says. `AREA(2)` answers 12.5663706144,
  `MAIN()` 3.1415926536, and the read-back differs from the source only in
  the trailing newline.
- Beyond the three errors 09-CONTEXT found, the path also: calls the six
  labels along the bottom soft keys that `GETKEY` reports as 0, 5, 10, 1, 6
  and 11, where
  [interface.soft-labels-not-keys](../../../docs/topics/interface.md#interface.soft-labels-not-keys)
  measured that touching them reports nothing and those codes are physical
  keys; says in step 4 that one soft-key code is still ambiguous, which no
  fact says; says in step 3 that `[Num]` and `[View]` never reach a blank
  app, where
  [apps.blank-app-keys](../../../docs/topics/apps.md#apps.blank-app-keys)
  measured them arriving as 9 and 11; states `main.py` as a requirement where
  [apps.main-py](../../../docs/topics/apps.md#apps.main-py) is `unverified`;
  says a PC run shows what the calculator will do, which is what
  `hpprime compare` exists to check; says the Virtual Calculator comes with
  the Connectivity Kit, where this machine had the CK without it; gives thirty
  bridge crossings as 8 ms, where the fact says 30 to 40; declares ten locals
  as its example of too many, where 9 to 12 is the range nobody has run
  ([ppl.local-limit](../../../docs/topics/ppl.md#ppl.local-limit)), naming
  `e` and `i` among them
  ([ppl.i-e-as-locals](../../../docs/topics/ppl.md#ppl.i-e-as-locals));
  says `L(0)` is a run-time error, where what was measured is `MID` with a 0;
  says a big matrix makes a function crawl, where the cost was never timed;
  says everything here was measured on a G2; draws with `TEXTOUT_P` without
  its width two lines before telling the reader to pass it; and quotes the
  linter's rules as all measured errors, which since 9704074 they are not.
- **The starter exports `AREA`**, which is also the Function app's `AREA`,
  and a reset calculator has the Function app active
  ([apps.reset-leaves-function-active](../../../docs/topics/apps.md#apps.reset-leaves-function-active)).
  What Home answers to `AREA(2)` then is not measured
  ([ppl.global-namespace](../../../docs/topics/ppl.md#ppl.global-namespace)
  says so), and the path promises 12.566. **The user chose on 2026-09-23 to
  rename it** rather than measure: `CIRCAREA`, which is not on the list.
- **`ppl.one-based` says an index of 0 is an error for lists and matrices**,
  while since 9704074 the linter and `tools.md` say that case is not
  measured: the evidence is `MID("abcdef",0,2)`. The fact says more than its
  evidence, and the path links to it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The check reaches docs/start/</name>
  <files>hpkit/docs.py, tests/test_reference.py</files>
  <action>`_layer_files()` walks `docs/start/` beside `commands/` and
  `topics/`, so its links, the facts it names, the layer above and the
  planning words are checked there too. Two breaks: a step pointing at
  `AGENTS.md`, and a step speaking of "this kit".</action>
  <verify><automated>python tests/test_reference.py</automated></verify>
</task>

<task type="auto">
  <name>Task 2: The starter exports CIRCAREA</name>
  <files>templates/starters/program.txt, hpkit/cli.py, hpkit/compare.py,
  tests/test_examples.py, tests/test_cli.py, docs/tools.md,
  docs/topics/ppl.md, README.md</files>
  <action>Rename the starter's `AREA` to `CIRCAREA`, and every command line
  that calls it: the `Next:` that `hpprime new` prints, the tests of the
  starter, the examples in `tools.md` and `compare.py` that use it as the
  sample library, `ppl.home-no-parentheses`'s example, and the one line of
  the README that runs it. `ppl.global-namespace` keeps `AREA` as its example
  of a collision, which is now the reason for the rename.</action>
  <verify><automated>python tests/test_examples.py; python tests/test_cli.py</automated></verify>
</task>

<task type="auto">
  <name>Task 3: ppl.one-based says what was measured</name>
  <files>docs/topics/ppl.md</files>
  <action>The rule stays: indexing starts at 1. An index of 0 is an error for
  `MID`, measured on the G2 and the emulator; what it does to a list or a
  matrix has not been measured, and `hpprime lint` warns on it as
  `unverified`. No new label; the claim narrows to its evidence.</action>
  <verify><automated>python hpprime.py docs --check</automated></verify>
</task>

<task type="auto">
  <name>Task 4: The six steps</name>
  <files>docs/start/*.md</files>
  <action>Rewrite each step in its order. Where the path uses a fact, it says
  what to do and links the fact, rather than restating its evidence. Every
  error listed above goes. Step 2's table of first-day failures gains a column
  for the fact and one for what `hpprime lint` does about it, and a single `=`
  leaves it for a sentence saying it compiles and compares. Step 6 sends a
  model to `docs/llms.txt` and teaches citing by identifier. No anecdote, no
  "this kit". Every program keeps linting clean.</action>
  <verify><automated>python hpprime.py docs --check</automated></verify>
</task>

<task type="auto">
  <name>Task 5: The path's programs are held clean</name>
  <files>tests/test_examples.py</files>
  <action>For each step, the starter and every `ppl` block that exports a
  function are linted together, and must give no error and no warning; a
  block that is a fragment is linted inside a function.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
</task>

</tasks>

<verification>
The whole suite passes; `hpprime docs --check` reports no problem with
`docs/start/` inside it; every command the path gives is run once more in a
scratch folder after the rewrite.
</verification>
