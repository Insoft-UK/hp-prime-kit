---
phase: 09-guided-path-index-and-readme
plan: 02
type: execute
depends_on: [09-01]
autonomous: true
requirements: [READ-04]
must_haves:
  truths:
    - "No page of the documentation speaks of a phase or of this kit, and the check refuses both"
    - "Keypresses as a cost, Prime_1 as the place a batch ran, rounds of work and the maintainer as the user are rewritten wherever they mean that"
    - "format.md explains batch, harness and probe once"
---

<objective>
Decisions 4 and 5 of `09-CONTEXT.md`: the documentation reads without
knowing how it was built.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: The check</name>
  <files>hpkit/docs.py, tests/test_reference.py</files>
  <action>`PLANNING` beside `ABOVE`: "phase" in any form, and "this kit" or
  "the kit", refused on every page of the documentation, `docs/llms.txt`
  included. Two breaks the suite must catch.</action>
</task>

<task type="auto">
  <name>Task 2: The rewrite</name>
  <files>the pages the scan lists</files>
  <action>Dump every line holding one of the words, with its neighbours,
  and rewrite each by reading: a phase goes, or becomes the group or the
  page the sentence means; "this kit" becomes "here", "this documentation",
  or the tool meant (the interpreter, the decoder, `hpprime build`). The
  legitimate uses stay: a round trip, keys a program waits for, the user of a
  program, and `Prime_1` as the emulator's own name in `deploy.md`. Every
  replacement is scripted and must match exactly; a scan over whole
  paragraphs catches a word split across two lines.</action>
</task>

<task type="auto">
  <name>Task 3: The glossary</name>
  <files>docs/format.md, docs/tools.md</files>
  <action>A section on how the examples were run: batch, harness, probe,
  and keys pressed by hand where a batch cannot reach.</action>
</task>

</tasks>

<verification>
`hpprime docs --check` reports no problem, a scan across line breaks finds
nothing, and the whole suite passes.
</verification>
