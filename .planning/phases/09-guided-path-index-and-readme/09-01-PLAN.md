---
phase: 09-guided-path-index-and-readme
plan: 01
type: execute
depends_on: []
autonomous: true
requirements: [READ-03, CHECK-02]
files_modified:
  - hpkit/docs.py
  - hpkit/examples.py
  - tests/test_reference.py
  - tests/test_examples_run.py
  - docs/llms.txt
  - docs/format.md
  - docs/tools.md
  - docs/topics/ppl.md
  - docs/commands/branch/CASE.md, IF.md, IFERR.md
  - docs/commands/loop/CONTINUE.md, REPEAT.md, WHILE.md
  - docs/commands/block/RETURN.md
  - docs/commands/strings/DIM.md, INSTRING.md
  - docs/commands/variable/LOCAL.md
  - the generated pages those entries appear in
must_haves:
  truths:
    - "docs/llms.txt lists every entry and every fact, one line each, with its link and its one-line summary"
    - "The check fails when docs/llms.txt is out of date, and when it would pass 100,000 bytes"
    - "The check fails on an example with no stored answer that is not *no value*, not G2, and not run through the interpreter"
    - "--relabel moves unverified to emulator where the stored answer agrees, and never moves G2"
    - "No entry says a single = is not a comparison, and none says an example is waiting for a batch it has already had"
---

<objective>
The index a model loads first, the check that no example goes unrun, and the
21 examples whose labels understate what was measured.
</objective>

<context>
@.planning/phases/09-guided-path-index-and-readme/09-CONTEXT.md (decisions 1 to 3)
@hpkit/docs.py      generated(), stale(), check(), results_check(), run_examples()
@hpkit/examples.py  relabel()
@docs/format.md     "How each claim is known" and "What is generated"
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/llms.txt</name>
  <files>hpkit/docs.py, tests/test_reference.py</files>
  <action>A fact keeps its heading when it is read. `generated()` adds
  `docs/llms.txt`: a title, a quoted line on what the reference is, a
  paragraph on the four labels, one on `names.tsv` as the place to look up
  whether a name exists, and a note that the file is generated; then a
  section per topic page, most used first, opening with the page's first
  sentence and listing each fact as its identifier linked to its anchor and
  its heading; then a section per group, each entry as its name linked to its
  file and its summary. `stale()` covers it with no change. The file joins
  the pages whose links and whose pointers to the layer above are checked.
  The check reports it past `LLMS_BUDGET`, 100,000 bytes, measured on what
  would be generated. Tests: every entry and fact has its line; a hand edit
  is caught as out of date; a summary long enough to pass the budget is
  caught.</action>
  <verify><automated>python tests/test_reference.py</automated></verify>
</task>

<task type="auto">
  <name>Task 2: The check for examples nobody ran</name>
  <files>hpkit/docs.py, tests/test_reference.py</files>
  <action>`run_examples()` can report which examples the interpreter
  actually ran. `results_check()` reports an example with no stored answer
  unless it is `*no value*`, labelled `G2`, or among those. The test for an
  uncovered example gives it a stored answer, since it now needs one; a new
  break adds an unrun `HP help` example to an entry that does not run on the
  PC.</action>
  <verify><automated>python tests/test_reference.py</automated></verify>
</task>

<task type="auto">
  <name>Task 3: --relabel moves unverified too</name>
  <files>hpkit/examples.py, tests/test_examples_run.py, docs/format.md, docs/tools.md</files>
  <action>`relabel()` takes `HP help` and `unverified` to `emulator` where
  the stored answer agrees; `G2` is untouched. Its test gains an
  `unverified` example with an agreeing row. The format and the tools page
  say so.</action>
  <verify><automated>python tests/test_examples_run.py</automated></verify>
</task>

<task type="auto">
  <name>Task 4: The 21, and the prose around them</name>
  <files>the ten entries, docs/topics/ppl.md</files>
  <action>Run the relabel on the documentation. Read each of the ten entries
  and bring its Behaviour into line with what is now labelled: `IF` and
  `WHILE` stop saying a single `=` is not a comparison; `LOCAL` and
  `INSTRING` stop saying their examples wait for a batch; a paragraph whose
  only support is a relabelled example takes the `emulator` label. Decide
  `ppl.locals-initialised-one-line` from the stored row: two locals
  initialised on one line compiled as a function body and answered 5, so the
  hypothesis is refuted on the emulator for two, and the fact says that more
  than two has not been run. Regenerate the pages.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
</task>

</tasks>

<verification>
The whole suite passes; `hpprime docs --check` reports no problem; the size
of `docs/llms.txt` is reported against its budget in the summary.
</verification>
