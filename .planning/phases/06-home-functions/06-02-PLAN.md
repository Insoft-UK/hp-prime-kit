---
phase: 06-home-functions
plan: 02
type: execute
wave: 2
depends_on: [01]
autonomous: false
requirements: [CMD-09]
files_modified:
  - docs/commands/matrix/*.md
  - docs/commands/results.tsv
must_haves:
  truths:
    - "The 40 matrix functions have an entry"
    - "An entry whose answer is a matrix states it in the shape the harness brings back"
  artifacts:
    - "entries for ABS through valuation"
  key_links:
    - "each links to ppl.one-based, ppl.index-call and ppl.type-codes where they apply"
---

<objective>
The 40 matrix functions, which join the 12 matrix commands Phase 5 wrote in
the same folder. It is the largest single group of the phase and the one
where the answers are richest: matrices, lists of eigenvalues, decompositions
that answer several objects at once.
</objective>

<context>
@.planning/phases/06-home-functions/06-CONTEXT.md
@.planning/phases/06-home-functions/06-01-SUMMARY.md
@docs/commands/matrix/   (the 12 commands, for the shape and the links)
</context>

<blocked>
The interpreter implements 5 of the 40 -- ABS, DET, IDENMAT, RREF, TRN -- and
HP's help gives an example with a result for those same five. The other 35
wait for the batch, which is the order the phase starts in.
</blocked>

<tasks>

<task type="auto">
  <name>Task 1: The five that can be drafted</name>
  <files>docs/commands/matrix/</files>
  <action>ABS, DET, IDENMAT, RREF, TRN from HP's syntax and its worked
  examples, labelled HP help until the batch moves them, and run through the
  interpreter so the tests hold them.</action>
  <verify><automated>python hpprime.py docs --check</automated></verify>
  <done>5 entries pass the format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>One batch with the examples of all 40: ABS COLNORM COND CROSS
  DET DOT EIGENVAL EIGENVV IDENMAT JordanBlock LQ LSQ LU ListToMat MAKEMAT QR
  RANDMAT RANK ROWNORM RREF SCHUR SPECNORM SPECRAD SVD SVL TRACE TRN cholesky
  diag hessenberg hilbert ihermite ismith jordan l1norm l2norm maxnorm mkisom
  pivot valuation. RANDMAT answers something different every run, so it is a
  probe for the shape of the answer rather than an example.</what-built>
  <how-to-verify>The usual three steps in the Prime_1 window.</how-to-verify>
  <resume-signal>"hecho", or what the screen said</resume-signal>
</task>

<task type="auto">
  <name>Task 2: The other 35, from what it answered</name>
  <files>docs/commands/matrix/, docs/commands/results.tsv</files>
  <action>Write them from the batch. The decompositions -- LU, QR, SVD, SCHUR,
  LQ -- answer more than one object, and how the harness brings that back is
  the thing to state precisely rather than paraphrase. A disagreement with
  HP's stated result is recorded in the entry, not resolved in its favour.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>40 entries, every suite passing.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes and `hpprime docs --check` reports no
problems, with the 40 matrix functions written.
</verification>

<success_criteria>
Criteria 1, 2 and 3 of Phase 6, for the matrix functions.
</success_criteria>
