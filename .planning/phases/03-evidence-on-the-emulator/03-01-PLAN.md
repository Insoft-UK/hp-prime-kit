---
phase: 03-evidence-on-the-emulator
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [EVID-01, TOOL-03]
files_modified:
  - hpkit/emulator.py
  - hpkit/cli.py
  - tests/test_program.py
  - tests/test_numbers.py
  - tests/test_emulator.py
  - docs/tools.md
must_haves:
  truths:
    - "hpprime doctor finds this machine's Connectivity Kit, whose folder is Calculadoras, and says which folder it used"
    - "It finds the Virtual Calculator and says which folder it used"
    - "The suites that read your own binaries find them in a localised Connectivity Kit too"
    - "The reader's wrong pick between two source records ending at the same offset is covered by a test"
  artifacts:
    - "one function that finds the Connectivity Kit's calculators folder by what it holds"
  key_links:
    - "doctor, test_program and test_numbers use that one function instead of three copies of the path"
---

<objective>
Find the Connectivity Kit's calculators folder whatever its name, in one
place that doctor and the tests share, and make sure the reader's fix for the
case TermoHP reported is held by a test.
</objective>

<context>
@.planning/phases/03-evidence-on-the-emulator/03-CONTEXT.md
@hpkit/cli.py        (_doctor looks for HP Connectivity Kit/Calculators)
@hpkit/emulator.py   (find_root, for the Virtual Calculator)
@hpkit/program.py    (_source_record: the innermost candidate wins)
On this machine: Documents/HP Connectivity Kit/ holds Calculadoras, Contenido,
Copias de seguridad, Documentación, Firmware, Idioma, Resultados, Temporal.
Documents/HP Prime/ holds Calculators (in English) and Spanish siblings.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Connectivity Kit's folder, found by what it holds</name>
  <files>hpkit/emulator.py, hpkit/cli.py, tests/test_program.py,
  tests/test_numbers.py, tests/test_emulator.py</files>
  <action>A function next to find_root: inside Documents/HP Connectivity Kit,
  the folder whose own folders are calculators, recognised by what a
  calculator folder holds (calc.hpsettings, .hpappdir folders, .hpprgm
  files), whatever the folder is called; HPPRIME_CK_ROOT overrides it, as
  HPPRIME_EMU_ROOT does for the emulator. doctor prints the folder it used,
  for both the Kit and the emulator. test_program and test_numbers use the
  function instead of their own copy of the path. A test builds a Kit folder
  with a Spanish name in a temporary directory and requires it to be
  found.</action>
  <verify><automated>python hpprime.py doctor</automated></verify>
  <done>doctor names C:\Users\<you>\Documents\HP Connectivity Kit\Calculadoras
  and the emulator's Calculators, and the suites pass.</done>
</task>

<task type="auto">
  <name>Task 2: The reader's wrong pick, held by a test</name>
  <files>tests/test_program.py</files>
  <action>If no test builds the case, add one: a container whose source
  length makes the wrapping record read as text too (32,737 characters
  against the shipped template is one), which must read back the source
  exactly and write back a file that reads the same. It must fail with the
  pre-fix rule (the largest candidate) and pass with the innermost one.
  Record in the summary which commit brought the fix.</action>
  <verify><automated>python tests/test_program.py</automated></verify>
  <done>The case is in a test, and the test passes.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes; `hpprime doctor` names both folders.
</verification>

<success_criteria>
Criteria 1 and 4 of Phase 3: both folders found and named, and the reader's
case held by a test.
</success_criteria>
