---
phase: 03-evidence-on-the-emulator
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
autonomous: false
requirements: [EVID-02, EVID-03, EVID-04]
files_modified:
  - docs/commands/results.tsv
  - docs/commands/strings/LEFT.md
  - docs/commands/strings/RIGHT.md
  - docs/commands/strings/MID.md
  - docs/commands/list/SIZE.md
  - docs/commands/loop/FOR.md
  - docs/reference/deploy.md
must_haves:
  truths:
    - "The 23 examples of the five sample entries have been run on the Virtual Calculator 2.4.15515, and their answers are in results.tsv"
    - "Every disagreement between the emulator and an entry is either resolved in the entry or listed as open"
    - "Which calculator a second emulator opens, with Prime in use, is measured and written down"
  artifacts:
    - "results.tsv with the first batch"
  key_links:
    - "the sample entries' labels follow the results"
---

<objective>
The first real batch: the five sample entries, with the user at the
emulator. It proves the whole path, and it answers the questions the samples
leave open.
</objective>

<context>
@.planning/phases/03-evidence-on-the-emulator/03-CONTEXT.md
@.planning/phases/03-evidence-on-the-emulator/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the batch</name>
  <action>Add to the batch the two open questions: RIGHT with a negative
  count, and TYPE of SIZE of a matrix. Create DOCS if needed, reset it,
  install, and launch the emulator with Prime still open.</action>
  <verify><automated>hpprime examples LEFT RIGHT MID SIZE FOR (up to the wait)</automated></verify>
  <done>The program is on DOCS and an emulator is starting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>A batch of the sample examples installed on the calculator
  DOCS, and an emulator launched while Prime stays open.</what-built>
  <how-to-verify>In the emulator that has just opened: check the title or
  the Home screen says it is DOCS; [Shift][Program], pick HPKDOC, Edit,
  Check, Esc; type HPKDOC on Home, Enter; close that emulator's window.</how-to-verify>
  <resume-signal>"hecho", or what the screen said if anything else happened</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Read it back, and let the entries answer</name>
  <action>Collect; confirm from calc.hpsettings that the emulator was DOCS;
  write the rows; run hpprime docs --check; resolve each disagreement in its
  entry or list it as open; relabel what the emulator confirms; write what
  was measured about which calculator a second emulator opens into
  docs/reference/deploy.md until Phase 4 moves it.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>The suite passes with the results in place, and the open questions
  have answers or are listed.</done>
</task>

</tasks>

<verification>
`hpprime docs --check` passes with results.tsv holding the first batch.
</verification>

<success_criteria>
Criterion 2 of Phase 3 shown on the real emulator, and criterion 3 with real
results.
</success_criteria>
