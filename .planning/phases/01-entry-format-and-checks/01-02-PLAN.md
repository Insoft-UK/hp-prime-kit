---
phase: 01-entry-format-and-checks
plan: 02
type: execute
wave: 2
depends_on: [01]
autonomous: true
requirements: [CMD-07, CHECK-01, CHECK-03, CHECK-05]
files_modified:
  - hpkit/docs.py
  - hpkit/cli.py
  - tests/test_reference.py
  - tests/test_hpdocs.py
  - tests/test_docs.py
  - tests/run_all.py
  - docs/commands/strings.md
  - docs/commands/lists.md
  - docs/commands/loops.md
  - docs/commands/index.md
  - docs/tools.md
must_haves:
  truths:
    - "The format in docs/format.md is enforced by a test, in both directions: what breaks it is caught, what follows it passes"
    - "Group pages and the index are generated, and a test fails when they are stale"
    - "Examples of commands the interpreter implements run through it and give the stated result"
    - "Nothing in the new documentation mentions the kit layer"
  artifacts:
    - hpkit/docs.py
    - "`hpprime docs` and `hpprime docs --check`"
    - tests/test_reference.py, registered in tests/run_all.py
  key_links:
    - "test_hpdocs.py and hpkit/docs.py share one parser for a result as the calculator prints it"
---

<objective>
Make the format enforceable: a standard-library module that reads entries and
facts, checks them, runs their examples through the interpreter, and generates
the group pages and the index; a command that runs it; and a test suite that
fails on every way the format can be broken.
</objective>

<context>
@.planning/phases/01-entry-format-and-checks/01-CONTEXT.md
@.planning/phases/01-entry-format-and-checks/01-01-PLAN.md  (the format)
@tests/test_hpdocs.py   (parse() and same(): a printed result against a value)
@tests/test_docs.py     (the link check, which already walks every .md)
@hpkit/cli.py           (how a command is wired into hpprime)
.gitattributes stores text as LF and may check it out as CRLF: every
comparison of a generated page reads it with newlines normalised.
</context>

<tasks>

<task type="auto">
  <name>Task 1: hpkit/docs.py</name>
  <files>hpkit/docs.py, tests/test_hpdocs.py</files>
  <action>Parse command entries and facts per the format; report problems as
  `file:line: message` (missing or misordered field, title not the file name,
  group not the folder, an example without exactly one label, a behaviour
  paragraph without a label, a fact whose anchor and Identifier differ,
  duplicate identifiers, a link that does not resolve, a mention of the kit
  layer). Compute each entry's weakest label. Run the examples of entries that
  say `Runs on the PC: yes` through hpkit.interp, the way test_hpdocs does, and
  compare with the stated result. Generate the group pages (entries in full,
  headings shifted one level, links rebased) and the index (name, group,
  summary, weakest label). Move parse() and same() here and have
  test_hpdocs.py import them.</action>
  <verify><automated>python tests/test_hpdocs.py</automated></verify>
  <done>The module reads the samples with no problems, and test_hpdocs gives
  the same result as before the move.</done>
</task>

<task type="auto">
  <name>Task 2: hpprime docs, and the generated pages</name>
  <files>hpkit/cli.py, docs/commands/strings.md, docs/commands/lists.md,
  docs/commands/loops.md, docs/commands/index.md, docs/tools.md</files>
  <action>Add `hpprime docs` (regenerate, say what changed) and `hpprime docs
  --check` (exit 1 on any problem or stale page). Generate the pages and
  commit them. Document the command in docs/tools.md.</action>
  <verify><automated>python hpprime.py docs --check</automated></verify>
  <done>The generated pages exist and --check exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: The tests</name>
  <files>tests/test_reference.py, tests/run_all.py, tests/test_docs.py</files>
  <action>A suite that passes on the real documentation and, in a temporary
  copy, catches each break: a duplicate identifier, a link that does not
  resolve, a page that mentions the kit, a stale generated page, an example
  with no label, a behaviour paragraph with no label, and an example whose
  result the interpreter contradicts. Register it in run_all.py. Add
  docs/format.md and docs/commands/index.md to test_docs.py's required entry
  points.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>Every suite passes, and each break above is caught by a named
  case.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes, and `python hpprime.py docs --check` exits 0.
</verification>

<success_criteria>
Breaking the format, an identifier, a link, the direction of the layers or a
generated page fails a test; the samples pass.
</success_criteria>
