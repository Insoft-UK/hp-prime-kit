---
phase: 04-facts-carried-over
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
autonomous: true
requirements: [FACT-01, FACT-05, CMD-06, TOOL-02, CHECK-04]
files_modified:
  - docs/topics/deploy.md
  - docs/topics/libraries.md
  - docs/reference/deploy.md
  - docs/reference/libraries.md
  - .planning/phases/04-facts-carried-over/MIGRATION.md
  - hpkit/lint.py
  - tests/test_lint.py
  - docs/commands/list/SIZE.md
  - docs/commands/strings/LEFT.md
must_haves:
  truths:
    - "Every lint message names the fact identifier its rule comes from"
    - "A test fails when a rule has no fact, or names one that does not exist"
    - "The deploy topic explains the send from the Connectivity Kit's content library, done once on 2026-09-09"
    - "The mistakes models are known to make are in the entries they concern"
    - "The migration list has a row for all 55 sections, none of them blank"
  artifacts:
    - docs/topics/deploy.md
    - docs/topics/libraries.md
    - .planning/phases/04-facts-carried-over/MIGRATION.md
  key_links:
    - "hpkit/lint.py rules cite facts in docs/topics/"
---

<objective>
The last two pages, and the tie between what the linter says and the fact it
learned it from.
</objective>

<context>
@.planning/phases/04-facts-carried-over/04-CONTEXT.md
@.planning/phases/04-facts-carried-over/04-02-SUMMARY.md
@docs/reference/deploy.md    (265 lines, 8 sections)
@docs/reference/libraries.md (159 lines, 4 sections: mostly advice)
@hpkit/lint.py               (thirteen rules)
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy and libraries</name>
  <files>docs/topics/deploy.md, docs/topics/libraries.md,
  docs/reference/deploy.md, docs/reference/libraries.md,
  .planning/phases/04-facts-carried-over/MIGRATION.md</files>
  <action>Deploy: the emulator's folder, the mirror that is not a mailbox, the
  refused drag, compiling on arrival, reading back what you installed, the
  writer against hardware, getting a template, which calculator is which, and
  the lock files. FACT-05: the send from the Connectivity Kit's content
  library, by hand, marked as done once on 2026-09-09. Libraries is mostly
  advice: the facts about published libraries become facts, the rest stays
  prose in the introduction, and the migration list says so for every
  section.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>docs/reference/ is empty, and every one of the 55 sections has a row
  with a destination.</done>
</task>

<task type="auto">
  <name>Task 2: Every lint message names its fact</name>
  <files>hpkit/lint.py, tests/test_lint.py</files>
  <action>Each rule carries the identifier of the fact it comes from, and
  prints it with the message. A rule with no fact fails the test, and so does
  one naming an identifier no topic page defines: the test reads the topics
  the way hpkit/docs.py does. Where a rule has no fact because none is
  written, the rule says which fact it is waiting for rather than inventing
  one.</action>
  <verify><automated>python tests/test_lint.py</automated></verify>
  <done>Thirteen rules, thirteen identifiers, checked against the topic
  pages.</done>
</task>

<task type="auto">
  <name>Task 3: What models get wrong</name>
  <files>docs/commands/list/SIZE.md, docs/commands/strings/LEFT.md,
  docs/topics/*.md</files>
  <action>CMD-06: the mistakes with a record go in the entry they concern, with
  what gets written, what happens, and where it was seen. Today there are two
  with a record -- SIZE of a matrix, and LEFT with a count of 0 -- plus what
  this milestone has caught: LEFT with a negative count, taken from HP's help
  and wrong on the calculator. Nothing is invented: an entry with no record
  gets no such section.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>Every suite passes with the new sections in place.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes, `hpprime docs --check` reports no problems,
`docs/reference/` is gone, and `hpprime lint` on a file that breaks a rule
prints the fact identifier.
</verification>

<success_criteria>
Criteria 1, 3, 4 and 5 of Phase 4, and criterion 2 completed for the last two
pages.
</success_criteria>
