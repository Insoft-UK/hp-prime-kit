---
phase: 04-facts-carried-over
plan: 02
type: execute
wave: 2
depends_on: [01]
autonomous: true
requirements: [FACT-01, FACT-02, FACT-03, FACT-04]
files_modified:
  - docs/topics/interface.md
  - docs/topics/apps.md
  - docs/topics/micropython.md
  - docs/reference/interface.md
  - docs/reference/apps.md
  - docs/reference/micropython.md
  - .planning/phases/04-facts-carried-over/MIGRATION.md
must_haves:
  truths:
    - "The screen, the keyboard and the touch that arrives twice are facts with their evidence"
    - "The startup-view byte, the blank-app trap and the app hooks are facts"
    - "What MicroPython has and has not, and the bridge across eval(), are facts, and what is not measured says so"
    - "Every section of the three pages has a row in the migration list"
  artifacts:
    - docs/topics/interface.md
    - docs/topics/apps.md
    - docs/topics/micropython.md
  key_links:
    - "facts about one command link to that command's entry where it exists"
---

<objective>
The three pages about the machine as a program sees it: the screen and the
keys, the apps, and Python.
</objective>

<context>
@.planning/phases/04-facts-carried-over/04-CONTEXT.md
@.planning/phases/04-facts-carried-over/04-01-SUMMARY.md
@docs/reference/interface.md   (554 lines, 12 sections: the largest)
@docs/reference/apps.md        (309 lines, 9 sections)
@docs/reference/micropython.md (269 lines, 7 sections)
</context>

<tasks>

<task type="auto">
  <name>Task 1: interface</name>
  <files>docs/topics/interface.md, docs/reference/interface.md,
  .planning/phases/04-facts-carried-over/MIGRATION.md</files>
  <action>The geometry, the building blocks, TEXTOUT_P and its width, what
  INPUT does and does not do, the key codes, the touch that arrives twice, the
  windowed list, tables and forms, units, and what can be tested on the PC.
  The long key-code table is evidence for one fact, not twelve. Where a fact
  is about one command (TEXTOUT_P, INPUT), the fact names it, and Phase 5 will
  make the entry link back.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>interface is a topic page, and its sections are in the list.</done>
</task>

<task type="auto">
  <name>Task 2: apps and micropython</name>
  <files>docs/topics/apps.md, docs/topics/micropython.md,
  docs/reference/apps.md, docs/reference/micropython.md,
  .planning/phases/04-facts-carried-over/MIGRATION.md</files>
  <action>Apps: what is inside a .hpappdir, the startup-view byte, the icon,
  the two kinds, the hooks and the blank-app trap, installing, and what is not
  solved. MicroPython: what is there, the hpprime module, the bridge, the
  architecture that makes it useful, why an app closes by itself, and the
  section that is explicitly not measured, which becomes facts labelled
  unverified rather than disappearing.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>Both are topic pages, and their sections are in the list.</done>
</task>

<task type="auto">
  <name>Task 3: The links, again</name>
  <files>README.md, AGENTS.md, SKILL.md, docs/start/**, docs/tools.md,
  examples/**, templates/README.md, hpkit/*.py</files>
  <action>Repoint everything that named the three pages.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>Every suite passes; docs/reference/ holds two pages.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes and `hpprime docs --check` reports no
problems.
</verification>

<success_criteria>
Criteria 1 and 2 of Phase 4, for these three pages.
</success_criteria>
