---
phase: 04-facts-carried-over
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [FACT-01, FACT-02, FACT-03, FACT-04]
files_modified:
  - docs/topics/ppl.md
  - docs/topics/formats.md
  - docs/reference/ppl.md
  - docs/reference/formats.md
  - .planning/phases/04-facts-carried-over/MIGRATION.md
  - docs/commands/strings/LEFT.md
  - docs/commands/strings/RIGHT.md
  - docs/commands/strings/MID.md
  - docs/commands/list/SIZE.md
  - docs/commands/loop/FOR.md
  - tests/test_reference.py
must_haves:
  truths:
    - "Every section of ppl.md and formats.md has a row in the migration list saying where it went"
    - "Each fact has an identifier, one label, a statement and its evidence, and is stated once"
    - "The hypotheses measured false and the items not measured are facts with their own label"
    - "Every link that pointed at docs/reference/ppl.md or formats.md resolves again"
  artifacts:
    - docs/topics/ppl.md
    - docs/topics/formats.md
    - .planning/phases/04-facts-carried-over/MIGRATION.md
  key_links:
    - "the five existing entries link to facts, not to the old reference pages"
---

<objective>
The two pages the rest of the documentation leans on -- the language and the
file formats -- rewritten as topic pages, with every link repointed.
</objective>

<context>
@.planning/phases/04-facts-carried-over/04-CONTEXT.md
@docs/format.md            (what a fact and a topic page must look like)
@docs/topics/ppl.md        (three facts already in the format)
@docs/reference/ppl.md     (253 lines, 6 sections)
@docs/reference/formats.md (370 lines, 7 sections)
</context>

<tasks>

<task type="auto">
  <name>Task 1: The migration list, and ppl</name>
  <files>.planning/phases/04-facts-carried-over/MIGRATION.md, docs/topics/ppl.md,
  docs/reference/ppl.md</files>
  <action>Start MIGRATION.md with a row per section of all seven pages, its
  new home left blank where it is not this plan's. Rewrite ppl.md into
  docs/topics/ppl.md: the limits that break compilation, the refuted
  hypotheses, the run-time traps, the speed anchor, each as facts with their
  evidence; the syntax summary and "where to look things up" as the page's
  introduction or as prose inside the fact they belong to. Keep the three
  identifiers that already exist. Remove docs/reference/ppl.md in the same
  commit.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>Every section of ppl.md has a row, and the page is gone.</done>
</task>

<task type="auto">
  <name>Task 2: formats</name>
  <files>docs/topics/formats.md, docs/reference/formats.md,
  .planning/phases/04-facts-carried-over/MIGRATION.md</files>
  <action>The same for formats.md: the container, the compiled block, the
  internal number, .hpmat and its vectors, the symbol entries, the other files
  on the calculator, and what is not solved. The parts that are a procedure
  rather than a fact (how to verify it yourself) stay prose in the
  introduction.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>formats is a topic page and its sections are in the list.</done>
</task>

<task type="auto">
  <name>Task 3: Every link that pointed there</name>
  <files>docs/commands/**, docs/start/**, docs/tools.md, README.md, AGENTS.md,
  SKILL.md, examples/**, templates/README.md, hpkit/*.py, tests/test_reference.py</files>
  <action>Repoint every link to docs/reference/ppl.md and
  docs/reference/formats.md at the new anchors, including the G2 labels of the
  five entries and the docstrings in hpkit that name those pages.
  test_reference's fixtures follow the new anchors.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>Every suite passes and no link points at a page that is gone.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes, `hpprime docs --check` reports no problems,
and `docs/reference/` holds five pages, not seven.
</verification>

<success_criteria>
Criteria 1 and 2 of Phase 4, for these two pages.
</success_criteria>
