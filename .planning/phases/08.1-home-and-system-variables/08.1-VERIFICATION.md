---
phase: 08.1-home-and-system-variables
status: approved
verified: 2026-09-24
requirements: [CMD-12, FACT-06]
---

# Phase 8.1 verification: Home and system variables, and the open questions

Counted on 2026-09-24 after plan 08.1-04, from `docs.load()`, the list of
names and `results.tsv`.

## Criterion 1: every name the list files as `variable` has an entry

| | |
|---|---|
| names of kind `variable` | 65 |
| with an entry | **65** |
| groups | home-settings 9, system 9, home 1, common-app-mode 4, common-numeric-view 12, common-plot-view 30 |
| problems `hpprime docs --check` reports | **0** |

Each says what it holds on a reset calculator and whether a program can set
it; the shared settings say the Function app was active.

## Criterion 2: `GET` has an entry

`docs/commands/other/GET.md`: on HP's list because a release note names it,
refused bare, with a number and with `QPI`'s answer.

## Criterion 3: every example has a result on file, or says why not

| | |
|---|---|
| examples in the documentation | 973 |
| with the Virtual Calculator's answer | 952 |
| measured by hand on a G2 | 4 |
| *no value*, with the reason | 17 |
| run nowhere | **0** |

## Criterion 4: the open questions measured, or saying why not

Of the sixteen in the context, fifteen were measured on the emulator and one,
the line `Check` names, was read off the screen during a measured session.
Left open, and saying so: `ENDPROC`, never compiled; 3 and 5 initialised
variables in one `EXPORT`; and whether another app's settings override
Home's as the Function app's do, which a batch cannot select.

## Criterion 5: a measured lint case is an error, or leaves the linter

| Case | Now |
|---|---|
| 9 to 12 variables in one `LOCAL` | error, `emulator` |
| 2 to 6 initialised in one `EXPORT` | not flagged |
| a `LOCAL` in a nested block | not flagged |
| a function's `END` without `;` | error, `emulator` |
| `ENDCASE`, `ENDFUNC` | error, `emulator` |
| a 0 as an index into a list | a warning, `emulator`: legal, and the last element |
| a call indexed, on a name the file does not define | a warning, `emulator`: the file cannot say which kind the name is |
| `ENDPROC` | a warning, `unverified`, not measured |

Each has its case in `tests/test_lint.py`, which fails on an error whose label
is not `G2` or `emulator`.

## Requirements

| | |
|---|---|
| CMD-12 | complete, since 08.1-03 |
| FACT-06 | complete, with the three cases left open above saying so |

## Suite

`python tests/run_all.py`: **13,550 passed, 0 failed**, across 13 suites.
