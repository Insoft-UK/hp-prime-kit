---
phase: 08.1-home-and-system-variables
plan: 03
type: execute
depends_on: [08.1-02]
autonomous: false
requirements: [CMD-12, FACT-06]
files_modified:
  - docs/commands/results.tsv
  - docs/commands/common-plot-view/*.md, 30 entries
  - the entries of AComplex, AFormat, ADigits, AAngle, NumZoom, HAngle
  - the fact on drawing units, if the plot window moves them
  - the generated pages
must_haves:
  truths:
    - "Every name of common-plot-view has an entry, measured with the Function app active and saying so"
    - "Whether a drawing coordinate follows the plot window is in results.tsv"
    - "What AComplex, AFormat and ADigits do at 2 is in results.tsv, and their entries say it"
---

<objective>
The third session of Phase 8.1: the 30 names of the plot view, and the
questions 08.1-02 raised, in one batch of 69 calls.
</objective>

<context>
@.planning/phases/08.1-home-and-system-variables/08.1-02-SUMMARY.md

Shaped as batch 2. Every set row now catches its own refusal, since some of
these names may belong to one app only -- the parameter, polar and sequence
ranges -- and the Function app is active.

- **Does a drawing coordinate follow the plot window?**
  [interface.draw-units](../../../docs/topics/interface.md#interface.draw-units)
  says a drawing unit is ten pixels and the origin the middle of the screen,
  which is what the default window gives. `C→PX(0,0)` read plain, then with
  `Xmin` at 0, then with `Ymax` at 0.
- **The app's settings at 2**: `AComplex` against `(-4)^0.5`, `AFormat`
  against `STRING(1/3)`, and both `AFormat` and `ADigits` at 2 together.
- **`AAngle` at 0 against `SIN(90)`**, and `HAngle` at 2 against `SIN(100)`,
  which is 1 if 2 is gradians.
- **`NumZoom` set to 4**, since batch 2 set it to the value it held.

Whether another app's `AAngle` overrides Home's needs another app active,
which a batch cannot select; it stays open.

The run waits three hours, not the default half hour, through the harness's
own `timeout`.
</context>

<tasks>

<task type="checkpoint">
  <name>Task 1: The batch, on the emulator</name>
  <action>As before: `Check` on `HPKDOC`, run it from Home, close the
  window.</action>
</task>

<task type="auto">
  <name>Task 2: The entries, and what the questions settled</name>
  <action>Thirty entries from the rows, each saying the Function app was
  active; the answers into the entries and facts they touch; the interpreter
  only where a row settles something it does.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
</task>

</tasks>
