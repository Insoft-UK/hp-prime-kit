---
phase: 05-statements-and-program-commands
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [CMD-08]
files_modified:
  - docs/commands/block/*.md
  - docs/commands/branch/*.md
  - docs/commands/loop/*.md
  - docs/commands/variable/*.md
  - docs/commands/function/KEY.md
  - docs/commands/results.tsv
  - docs/topics/ppl.md
must_haves:
  truths:
    - "The 14 statements have an entry in the fixed format"
    - "Their examples have run on the Virtual Calculator, or the entry says why they cannot"
    - "ppl.equality-operators and ppl.end-semicolon are settled, and the two lint rules follow what the calculator said"
  artifacts:
    - "entries for BEGIN, KILL, RETURN, CASE, IF, IFERR, BREAK, CONTINUE, FOR, REPEAT, WHILE, EXPORT, LOCAL, KEY"
  key_links:
    - "each statement links to the facts in docs/topics/ppl.md it depends on"
---

<objective>
The language itself: every statement PPL has, written and run. It is the
smallest block and the one everything else leans on, so the format meets
real volume here first.
</objective>

<context>
@.planning/phases/05-statements-and-program-commands/05-CONTEXT.md
@docs/format.md
@docs/commands/loop/FOR.md      (the one statement already written)
@docs/topics/ppl.md             (the facts these entries link to)
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 14 entries</name>
  <files>docs/commands/block/, branch/, loop/, variable/, function/KEY.md</files>
  <action>BEGIN, KILL, RETURN; CASE, IF, IFERR; BREAK, CONTINUE, REPEAT,
  WHILE (FOR exists); EXPORT, LOCAL; KEY. Each from HP's syntax in names.tsv
  and its help example, labelled HP help until the batch moves it, linking to
  ppl.local-limit, ppl.locals-at-top, ppl.return-in-loop,
  ppl.function-always-answers, ppl.no-end-keywords and
  ppl.export-initialised where they apply. An example with a `;` is a
  function body, which the harness runs as a function of its own.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>14 entries pass the format, and the generated pages are rebuilt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>A batch with these entries' runnable examples and four probes:
  `IF a = 1 THEN` and a bare `END`, which settle the two facts the linter
  calls errors on; `BREAK 2` inside two nested loops, where the interpreter
  carried on with the outer body and HP documents leaving both; and `LOCAL
  za := 2, zb := 3`, which settles ppl.locals-initialised-one-line, resting
  today on a published tutorial.</what-built>
  <how-to-verify>The usual three steps in the window that opens, which will
  be Prime_1: Check the program once, run HPKDOC on Home, close that
  window.</how-to-verify>
  <resume-signal>"hecho", or what the screen said if anything else happened</resume-signal>
</task>

<task type="auto">
  <name>Task 2: What the calculator said</name>
  <files>docs/commands/**, docs/topics/ppl.md, hpkit/lint.py, docs/tools.md</files>
  <action>Collect, resolve every disagreement in its entry or list it as
  open, relabel what the emulator confirms. Then the two probes:
  ppl.equality-operators and ppl.end-semicolon get the emulator's label and
  the answer, and if the calculator accepts either form, the lint rule that
  called it an error is wrong and changes with it -- that is the point of
  having measured it.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>Every suite passes, and no entry states something the calculator
  contradicts.</done>
</task>

</tasks>

<verification>
`hpprime docs --check` reports no problems and `python tests/run_all.py`
passes, with 19 entries in the documentation.
</verification>

<success_criteria>
Criteria 1 and 2 of Phase 5, for the statements.
</success_criteria>
