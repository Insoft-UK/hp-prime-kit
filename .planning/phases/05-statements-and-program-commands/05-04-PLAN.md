---
phase: 05-statements-and-program-commands
plan: 04
type: execute
wave: 4
depends_on: [03]
autonomous: false
requirements: [CMD-08]
files_modified:
  - docs/commands/io/*.md
  - docs/commands/app/*.md
  - docs/commands/results.tsv
must_haves:
  truths:
    - "The 10 io commands and the 3 app commands have an entry"
    - "An entry whose command waits for a person says so, and says what it returns when it is answered"
  artifacts:
    - "entries for CHOOSE, CHOOSEDATE, EDITLIST, GETKEY, INPUT, ISKEYDOWN, MOUSE, MSGBOX, PRINT, WAIT, STARTAPP, STARTVIEW, VIEW"
  key_links:
    - "each links to the facts in docs/topics/interface.md that were measured about it"
---

<objective>
The commands that talk to a person. Most of them cannot be run by a batch
without somebody pressing a key, and the entries say so rather than
pretending.
</objective>

<context>
@.planning/phases/05-statements-and-program-commands/05-CONTEXT.md
@.planning/phases/05-statements-and-program-commands/05-03-SUMMARY.md
@docs/topics/interface.md   (what is already measured about INPUT, GETKEY, MOUSE and WAIT)
</context>

<tasks>

<task type="auto">
  <name>Task 1: The entries</name>
  <files>docs/commands/io/, docs/commands/app/</files>
  <action>CHOOSE, CHOOSEDATE, EDITLIST, GETKEY, INPUT, ISKEYDOWN, MOUSE,
  MSGBOX, PRINT, WAIT; STARTAPP, STARTVIEW, VIEW. Each links to what
  interface.md already holds: interface.input-fields, interface.input-modal,
  interface.getkey-position, interface.key-codes, interface.mouse-lists,
  interface.drain-then-wait, interface.wait-minus-one. What each returns when
  a person answers it is the part an example can state; that it waits is the
  reason most have no runnable example, and the entry says which of the two
  it is.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>13 entries pass the format, each saying what can and cannot be
  run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>A small batch: only the calls that answer without waiting, such
  as ISKEYDOWN and GETKEY with nothing pressed.</what-built>
  <how-to-verify>The usual three steps in the Prime_1 window.</how-to-verify>
  <resume-signal>"hecho", or what the screen said</resume-signal>
</task>

<task type="auto">
  <name>Task 2: What the calculator said</name>
  <files>docs/commands/**, docs/topics/interface.md</files>
  <action>Collect, settle, relabel. An answer that contradicts a fact in
  interface.md is a disagreement to resolve there, not quietly in the
  entry.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>Every suite passes.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes with 82 entries in the documentation.
</verification>

<success_criteria>
Criteria 1 and 2 of Phase 5, for io and app.
</success_criteria>
