---
phase: 05-statements-and-program-commands
plan: 02
type: execute
wave: 2
depends_on: [01]
autonomous: false
requirements: [CMD-08]
files_modified:
  - docs/commands/strings/*.md
  - docs/commands/integer/*.md
  - docs/commands/results.tsv
must_haves:
  truths:
    - "The 13 string commands and the 11 integer commands have an entry"
    - "Every example has an answer from the Virtual Calculator on file"
  artifacts:
    - "entries for ASC, CHAR, DIM, EXPR, INSTRING, LOWER, ROTATE, STRING, STRINGFROMID, UPPER, and the eleven integer commands"
  key_links:
    - "the string entries link to each other and to ppl.one-based"
---

<objective>
The commands that take a value and give a value back: the easiest to verify,
and the ones a model reaches for most.
</objective>

<context>
@.planning/phases/05-statements-and-program-commands/05-CONTEXT.md
@.planning/phases/05-statements-and-program-commands/05-01-SUMMARY.md
@docs/commands/strings/LEFT.md   (the shape these follow)
</context>

<tasks>

<task type="auto">
  <name>Task 1: strings</name>
  <files>docs/commands/strings/</files>
  <action>ASC, CHAR, DIM, EXPR, INSTRING, LOWER, ROTATE, STRING,
  STRINGFROMID, UPPER. LEFT, MID and RIGHT exist and are left alone except
  for links. The edges worth pinning: what INSTRING answers when it finds
  nothing and when the needle is empty (the statement is in the migration
  list, waiting for this entry), EXPR of an empty string
  (ppl.expr-empty), and what STRING does to a list and to a matrix, which
  the first batch already answered for lists.

  Six of them -- ASC, CHAR, LOWER, UPPER, ROTATE and STRINGFROMID -- cannot
  be written before the batch: the interpreter does not implement them, and
  HP's help gives no example with a result for any of them, so there is
  nothing to put in the Result column that would not be invented. They are
  written after the emulator has answered, and the batch carries their calls
  as probes. DIM, EXPR, INSTRING and STRING go first, because HP's help
  documents examples for the first three and STRING's list form is already
  measured in results.tsv.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>DIM, EXPR, INSTRING and STRING pass the format; the other six are
  written once the batch has answered.</done>
</task>

<task type="auto">
  <name>Task 2: integer</name>
  <files>docs/commands/integer/</files>
  <action>BITAND, BITNOT, BITOR, BITSL, BITSR, BITXOR, GETBITS, SETBASE,
  SETBITS, and the two whose names carry an arrow. HP's help gives a syntax
  for every one; the edges to pin are the base the answer comes back in and
  what a negative or fractional argument does.

  Measured 2026-09-12, before writing any of them: the interpreter
  implements none of the eleven, and HP's help dump holds no example with a
  result for any of them either. So not one of these entries can be written
  without inventing the Result column, and the whole group waits for the
  batch, like the six string commands. The batch carries their calls as
  probes; the entries are written from what it answers.</action>
  <verify><automated>python -m hpkit.cli docs --check</automated></verify>
  <done>11 integer entries pass the format.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>One batch with the examples of all 24 entries.</what-built>
  <how-to-verify>The usual three steps in the Prime_1 window.</how-to-verify>
  <resume-signal>"hecho", or what the screen said</resume-signal>
</task>

<task type="auto">
  <name>Task 3: What the calculator said</name>
  <files>docs/commands/**</files>
  <action>Collect, settle the disagreements, relabel what is confirmed.</action>
  <verify><automated>python tests/run_all.py</automated></verify>
  <done>Every suite passes.</done>
</task>

</tasks>

<verification>
`python tests/run_all.py` passes with 43 entries in the documentation.
</verification>

<success_criteria>
Criteria 1 and 2 of Phase 5, for strings and integers.
</success_criteria>
