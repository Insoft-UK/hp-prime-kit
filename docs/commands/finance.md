# Finance

<!-- Generated by `hpprime docs` from the entries in `finance/`. Edit those, not this page. -->

Every entry in this group, in full. Each one is written in its own file, and that file is where it is edited.

| | |
|---|---|
| [Accrued](#Accrued) | The accrued interest of a bond, 0 on a fresh calculator. |
| [BEG](#BEG) | Whether payments fall at the start of a period, 1 once Finance is active. |
| [BSCall](#BSCall) | The Black-Scholes value of a call, 0 while its twin BSPut refuses. |
| [BSPut](#BSPut) | The Black-Scholes value of a put, refused while its twin BSCall answers. |
| [BlackScholes](#BlackScholes) | Answers a list of two, for an option on a stock. |
| [Bond360](#Bond360) | Whether a bond counts a 360-day year. |
| [BondPrice](#BondPrice) | The price of a bond, refused in the form tried here. |
| [BondYield](#BondYield) | The yield of a bond, refused in the form tried here. |
| [BrkEvCost](#BrkEvCost) | The cost per unit, from fixed cost, quantity, price and profit. |
| [BrkEvFixed](#BrkEvFixed) | The fixed cost, from quantity, cost, price and profit. |
| [BrkEvPrice](#BrkEvPrice) | The price per unit, from fixed cost, quantity, cost and profit. |
| [BrkEvProfit](#BrkEvProfit) | The profit, from fixed cost, quantity, cost and price. |
| [BrkEvQuant](#BrkEvQuant) | The quantity, from fixed cost, cost, price and profit. |
| [CFData](#CFData) | The cash flows, an empty list until a program puts some in. |
| [CFPYR](#CFPYR) | Cash flows per year, 12 as the app arrives. |
| [CPYR](#CPYR) | Compounding periods per year, 12 once the Finance app is active. |
| [CallPrice](#CallPrice) | A bond’s call price, 100 as the app arrives. |
| [CashFlowFMRR](#CashFlowFMRR) | A break-even rate of the same family, which this example cannot tell from MIRR. |
| [CashFlowIRR](#CashFlowIRR) | The rate at which a list of cash flows breaks even. |
| [CashFlowMIRR](#CashFlowMIRR) | A break-even rate that separates the borrowing rate from the reinvestment one. |
| [CashFlowNFV](#CashFlowNFV) | What a list of cash flows is worth at the end, at a given rate. |
| [CashFlowNPV](#CashFlowNPV) | What a list of cash flows is worth today, at a given rate. |
| [CashFlowNUS](#CashFlowNUS) | The level amount per period worth the same as the flows. |
| [CashFlowPB](#CashFlowPB) | How many periods it takes to get the money back. |
| [CashFlowTotal](#CashFlowTotal) | Adds up a list of cash flows, without discounting them. |
| [Change](#Change) | The percentage change between two values. |
| [ChangeCost](#ChangeCost) | The cost that a price and a percentage give. |
| [ChangeNew](#ChangeNew) | Answers something an ordinary reading of its arguments does not explain. |
| [ChangeOld](#ChangeOld) | Answers something an ordinary reading of its arguments does not explain. |
| [ChangePrice](#ChangePrice) | The price that a cost and a percentage give. |
| [Cost](#Cost) | The cost a margin or markup is taken on. |
| [CostAsset](#CostAsset) | The cost of an asset being depreciated. |
| [CpnPer](#CpnPer) | A bond’s coupon, 0 on a fresh calculator. |
| [Date360](#Date360) | Whether date arithmetic counts a 360-day year. |
| [DateDays](#DateDays) | The days between two dates, refused in the form tried here. |
| [DateDiff](#DateDiff) | The days between two dates, 0 while both are the same. |
| [DateOne](#DateOne) | The first date of a date calculation, 2017.0101 as the app arrives. |
| [DateTwo](#DateTwo) | The second date of a date calculation, the same 2017.0101. |
| [Depreciate](#Depreciate) | Depreciation over the life of an asset, refused in the form tried here. |
| [DiscPayback](#DiscPayback) | The discounted payback period, refused either way. |
| [Dividend](#Dividend) | The dividend yield in a Black-Scholes calculation. |
| [EffInt](#EffInt) | An effective interest rate. |
| [FMRR](#FMRR) | The financial management rate of return, refused either way. |
| [FV](#FV) | The future value, which needs the Finance app to be read. |
| [FactorDepr](#FactorDepr) | The declining-balance factor, 200 as the app arrives. |
| [FinType](#FinType) | A code for which finance calculation the app is set to. |
| [FirstAsset](#FirstAsset) | The first year of a depreciation, 1 as the app arrives. |
| [FirstDateAsset](#FirstDateAsset) | The date a depreciation starts, 2017.0101 as the app arrives. |
| [FixedCost](#FixedCost) | The fixed cost of a break-even calculation. |
| [GSize](#GSize) | The group size of an amortisation, 12 once Finance is active. |
| [IPYR](#IPYR) | The interest rate per year, which needs the Finance app to be read. |
| [IRR](#IRR) | The internal rate of return, refused with or without the Finance app. |
| [IntCPYR](#IntCPYR) | Compounding periods per year for interest conversion, 12 as the app arrives. |
| [IntConvCPYR](#IntConvCPYR) | How often a rate compounds, from the nominal and effective rates. |
| [IntConvEff](#IntConvEff) | The effective rate, from a nominal rate and how often it compounds. |
| [IntConvNom](#IntConvNom) | The nominal rate, from an effective rate and how often it compounds. |
| [InvestInt](#InvestInt) | The reinvestment rate a modified return uses. |
| [LifeAsset](#LifeAsset) | The life of an asset in years, 1 as the app arrives. |
| [MIRR](#MIRR) | The modified internal rate of return, refused either way. |
| [Macaulay](#Macaulay) | A bond’s Macaulay duration. |
| [Margin](#Margin) | A margin as a percentage of the sale price. |
| [Markup](#Markup) | A markup as a percentage of the cost. |
| [MatDate](#MatDate) | A bond’s maturity date, 2017.0101 as the app arrives. |
| [Modified](#Modified) | A bond’s modified duration. |
| [NFV](#NFV) | The net future value, refused with or without the Finance app. |
| [NPV](#NPV) | The net present value, refused with or without the Finance app. |
| [NUS](#NUS) | The net uniform series, refused with or without the Finance app. |
| [NbPmt](#NbPmt) | The number of payments, which needs the Finance app to be read. |
| [NewValue](#NewValue) | The new value of a percentage change. |
| [NomInt](#NomInt) | A nominal interest rate. |
| [OldValue](#OldValue) | The old value of a percentage change. |
| [PMT](#PMT) | The payment, which needs the Finance app to be read. |
| [PPYR](#PPYR) | Payments per year, 12 once the Finance app is active. |
| [PV](#PV) | The present value: needs the Finance app, and a program can set it. |
| [Payback](#Payback) | The payback period, refused with or without the Finance app. |
| [PercentChange](#PercentChange) | How far the new value is from the old one, as a percentage of the old. |
| [PercentMargin](#PercentMargin) | The difference between cost and price, over the cost. |
| [PercentMarkup](#PercentMarkup) | The difference between cost and price, over the price. |
| [PercentTotal](#PercentTotal) | The new value as a percentage of the old one. |
| [Price](#Price) | The unit price of a break-even calculation. |
| [PriceBond](#PriceBond) | A bond’s price. |
| [Profit](#Profit) | The profit of a break-even calculation. |
| [Quantity](#Quantity) | The units sold in a break-even calculation. |
| [RiskFree](#RiskFree) | The risk-free rate in a Black-Scholes calculation. |
| [SafeInt](#SafeInt) | The safe rate a modified return uses. |
| [SalePrice](#SalePrice) | The sale price of a margin or markup calculation. |
| [SalvageAsset](#SalvageAsset) | The salvage value of an asset being depreciated. |
| [SemiAnnual](#SemiAnnual) | Whether a bond pays its coupon twice a year. |
| [SetDate](#SetDate) | A bond’s settlement date, 2017.0101 as the app arrives. |
| [StockPrice](#StockPrice) | The stock price in a Black-Scholes calculation. |
| [StrikePrice](#StrikePrice) | The strike price in a Black-Scholes calculation. |
| [TimeMarket](#TimeMarket) | The time to expiry in a Black-Scholes calculation. |
| [Total](#Total) | A total, 0 on a fresh calculator. |
| [TotalCF](#TotalCF) | The total of the cash flows, 0 while there are none. |
| [TvmFV](#TvmFV) | The future value, from the other four. |
| [TvmIPYR](#TvmIPYR) | The interest rate a year, from the other four. |
| [TvmNbPmt](#TvmNbPmt) | How many payments, from the other four. |
| [TvmPMT](#TvmPMT) | The payment, from the other four. |
| [TvmPV](#TvmPV) | The present value, from the other four. |
| [VariableCost](#VariableCost) | The variable cost per unit of a break-even calculation. |
| [Volatility](#Volatility) | The volatility in a Black-Scholes calculation. |
| [YieldBond](#YieldBond) | A bond’s yield. |

---

<a name="Accrued"></a>

## Accrued

The accrued interest of a bond, 0 on a fresh calculator.

| | |
|---|---|
| Syntax | `Accrued` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Accrued")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the interest a bond has accrued since its last coupon** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PriceBond](finance/PriceBond.md) · [SetDate](finance/SetDate.md) · [CpnPer](finance/CpnPer.md)

---

<a name="BEG"></a>

## BEG

Whether payments fall at the start of a period, 1 once Finance is active.

| | |
|---|---|
| Syntax | `BEG` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BEG")` | *error* | [emulator](results.tsv) |
| `EXPR(" BEG")` | `1.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), [IPYR](finance/IPYR.md), [PV](finance/PV.md), [PMT](finance/PMT.md), [FV](finance/FV.md), [PPYR](finance/PPYR.md), [CPYR](finance/CPYR.md), `BEG` and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds a switch between payments at the start and the end of a period** (unverified): HP's list gives this name and its app, and
nothing more. It read `1.00` once Finance was active, a default the app arrives with.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PV](finance/PV.md) · [PPYR](finance/PPYR.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="BSCall"></a>

## BSCall

The Black-Scholes value of a call, 0 while its twin BSPut refuses.

| | |
|---|---|
| Syntax | `BSCall` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BSCall")` | `0` | [emulator](results.tsv) |
| `EXPR(" BSCall")` | `0.00` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the value of a call option by Black-Scholes** (unverified): HP’s list gives this name and its app,
and nothing more.

**Its twin [BSPut](finance/BSPut.md) refused under both conditions, and this did not**
(emulator). Every Black-Scholes input read 0 -- [StockPrice](finance/StockPrice.md),
[StrikePrice](finance/StrikePrice.md), [Volatility](finance/Volatility.md) -- so both values
are computed from nothing. That one answers 0 and the other refuses is the
difference this entry cannot explain, and a call with real inputs is the
probe (unverified).

**With the Finance app active it reads `0.00`, the same value with two decimals**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals). The decimals are the app's display, not part of the
value.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BSPut](finance/BSPut.md) · [StockPrice](finance/StockPrice.md) · [Volatility](finance/Volatility.md)

---

<a name="BSPut"></a>

## BSPut

The Black-Scholes value of a put, refused while its twin BSCall answers.

| | |
|---|---|
| Syntax | `BSPut` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BSPut")` | *error* | [emulator](results.tsv) |
| `EXPR(" BSPut")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the value of a put option by Black-Scholes** (unverified): HP's list gives this name and its app, and
nothing more.

**Its twin [BSCall](finance/BSCall.md) answered 0 under both conditions** (emulator),
with every Black-Scholes input at 0. Both values are computed from nothing,
and only this one refuses. Why is untested, and a call with real inputs --
a stock price, a strike, a volatility -- is the probe (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BSCall](finance/BSCall.md) · [StockPrice](finance/StockPrice.md)

---

<a name="BlackScholes"></a>

## BlackScholes

Answers a list of two, for an option on a stock.

| | |
|---|---|
| Syntax | `BlackScholes(stock_price,strike_price,time_to_maturity,risk_free_interest_rate,stock_volatility,stock_divi` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BlackScholes(100,95,1,5,0.2,0)")` | `{9.63320467243,0}` | [emulator](results.tsv) |

### Behaviour

`BlackScholes(100,95,1,5,0.2,0)` answers `{9.63320467243,0}` (emulator), a
list of type 6, [ppl.type-codes](../topics/ppl.md#ppl.type-codes), where
every other command in this group answers a single number.

**Two values come back and this entry does not say which is which**
(emulator). An option has a call price and a put price, and a list of two is
what a command answering both would give, but nothing measured here labels
the elements. The probe is a second call with the strike above the stock
price rather than below, which should move the two in opposite directions and
name them.

**The second element is exactly zero, which is worth suspicion rather than
belief** (emulator). A put on these terms should be worth something, so the
zero may mean the element is not a put at all, or that an argument the call
did not supply is needed before it computes.

**HP's own list is truncated** (HP help): the syntax row ends part way
through the sixth argument's name, so the full argument list is not published
in the list of names here. Six were sent because six are visible, and the
call was accepted, which says the visible six are enough to get an answer and
not that they are all of them.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNPV](finance/CashFlowNPV.md) · [TvmFV](finance/TvmFV.md) ·
[ppl.type-codes](../topics/ppl.md#ppl.type-codes)

---

<a name="Bond360"></a>

## Bond360

Whether a bond counts a 360-day year.

| | |
|---|---|
| Syntax | `Bond360` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Bond360")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds a switch between a 360-day and an actual year for bonds** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Date360](finance/Date360.md) · [PriceBond](finance/PriceBond.md) · [SemiAnnual](finance/SemiAnnual.md)

---

<a name="BondPrice"></a>

## BondPrice

The price of a bond, refused in the form tried here.

| | |
|---|---|
| Syntax | `BondPrice(settlement_date,maturity_date,coupon_percent,call_value,yield_percent,semi_annual,cal360)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BondPrice(1.012024,1.012034,5,100,6,2,0)")` | *error* | [emulator](results.tsv) |

### Behaviour

**The call is refused** (emulator), with all seven arguments in the published
order: two dates, a coupon of five per cent, a call value of a hundred, a
yield of six, semi-annual coupons and an ordinary calendar.

**It shares its refusal with the other two commands that take dates**
(emulator): [DateDays](finance/DateDays.md) and [BondYield](finance/BondYield.md) were
refused in the same batch with dates written the same way, while every
finance command taking only plain numbers answered. That pattern points at
the dates rather than at the bond arithmetic, and [DateDays](finance/DateDays.md)
carries the account and the probe.

**Nothing here measures the other six arguments** (unverified). If the date
shape is what is wrong, the rest of the list may be right as published; if it
is not, everything after the first two arguments is still untested. This
entry does not choose between those.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BondYield](finance/BondYield.md) · [DateDays](finance/DateDays.md)

---

<a name="BondYield"></a>

## BondYield

The yield of a bond, refused in the form tried here.

| | |
|---|---|
| Syntax | `BondYield(settlement_date,maturity_date,price,coupon_percent,call_value,semi_annual,cal360)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BondYield(1.012024,1.012034,95,5,100,2,0)")` | *error* | [emulator](results.tsv) |

### Behaviour

**The call is refused** (emulator), with the seven arguments in the published
order: two dates, a price of ninety-five, a coupon of five per cent, a call
value of a hundred, semi-annual coupons and an ordinary calendar.

**It is the inverse of [BondPrice](finance/BondPrice.md)**, which takes a yield and
answers a price where this takes a price and answers a yield (HP help). Both
were refused in the same batch, so neither could be used to check the other,
which is the round trip that would have settled the argument order the way
the `Tvm` and `BrkEv` families were settled.

**The dates are the shared suspect** (emulator): this, [BondPrice](finance/BondPrice.md)
and [DateDays](finance/DateDays.md) are the three finance commands taking dates and
the three that were refused, while the plain-number commands all answered.
[DateDays](finance/DateDays.md) carries the account and names the probe.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BondPrice](finance/BondPrice.md) · [DateDays](finance/DateDays.md)

---

<a name="BrkEvCost"></a>

## BrkEvCost

The cost per unit, from fixed cost, quantity, price and profit.

| | |
|---|---|
| Syntax | `BrkEvCost (fixed_cost, quantity, price, profit)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BrkEvCost(1000,100,20,500)")` | `5` | [emulator](results.tsv) |

### Behaviour

`BrkEvCost(1000,100,20,500)` answers 5 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It answers the cost of one unit, not of all of them** (emulator). The five
it returns times the hundred units is the five hundred of stock that
[BrkEvProfit](finance/BrkEvProfit.md) subtracts, so a program holding a total has to
divide before calling this or it will be out by the quantity.

The arguments omit the cost, which is what this one answers (HP help). HP's
own list writes a space between the name and its bracket; the calculator
takes the call without it, which is how the row above was sent.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BrkEvProfit](finance/BrkEvProfit.md) · [BrkEvQuant](finance/BrkEvQuant.md) ·
[BrkEvFixed](finance/BrkEvFixed.md) · [BrkEvPrice](finance/BrkEvPrice.md)

---

<a name="BrkEvFixed"></a>

## BrkEvFixed

The fixed cost, from quantity, cost, price and profit.

| | |
|---|---|
| Syntax | `BrkEvFixed(quantity, cost, price, profit)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BrkEvFixed(100,5,20,500)")` | `1000` | [emulator](results.tsv) |

### Behaviour

`BrkEvFixed(100,5,20,500)` answers 1000 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It returns the thousand the other four members were given** (emulator).
A hundred units at twenty, less a hundred at five, less the five hundred of
profit, leaves the thousand of fixed cost, which is the same equation
[BrkEvProfit](finance/BrkEvProfit.md) reads in the other direction.

This is the only member whose argument list starts with the quantity
(HP help), because it is the only one that does not need the fixed cost as an
input.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BrkEvProfit](finance/BrkEvProfit.md) · [BrkEvQuant](finance/BrkEvQuant.md) ·
[BrkEvCost](finance/BrkEvCost.md) · [BrkEvPrice](finance/BrkEvPrice.md)

---

<a name="BrkEvPrice"></a>

## BrkEvPrice

The price per unit, from fixed cost, quantity, cost and profit.

| | |
|---|---|
| Syntax | `BrkEvPrice(fixed_cost, quantity, cost, profit)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BrkEvPrice(1000,100,5,500)")` | `20` | [emulator](results.tsv) |

### Behaviour

`BrkEvPrice(1000,100,5,500)` answers 20 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It returns the twenty the other members were given** (emulator): the
thousand of fixed cost, the five hundred of stock and the five hundred of
profit come to two thousand, spread over a hundred units.

Like [BrkEvCost](finance/BrkEvCost.md), the answer is per unit rather than a total,
so a program holding revenue has to divide by the quantity before comparing
(emulator).

The arguments omit the price, which is what this one answers (HP help).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BrkEvProfit](finance/BrkEvProfit.md) · [BrkEvQuant](finance/BrkEvQuant.md) ·
[BrkEvCost](finance/BrkEvCost.md) · [BrkEvFixed](finance/BrkEvFixed.md)

---

<a name="BrkEvProfit"></a>

## BrkEvProfit

The profit, from fixed cost, quantity, cost and price.

| | |
|---|---|
| Syntax | `BrkEvProfit(fixed_cost, quantity, cost, price)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BrkEvProfit(1000,100,5,20)")` | `500` | [emulator](results.tsv) |

### Behaviour

`BrkEvProfit(1000,100,5,20)` answers 500 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**The arithmetic is visible in the answer** (emulator): a hundred units sold
at twenty, less a hundred bought at five, less a thousand of fixed cost, is
2000 minus 500 minus 1000, which is the 500 that came back. That is what says
the third argument is the cost per unit and not the total cost.

**Five names solve one equation, and this is the one for the profit**
(emulator): [BrkEvQuant](finance/BrkEvQuant.md), [BrkEvCost](finance/BrkEvCost.md),
[BrkEvFixed](finance/BrkEvFixed.md) and [BrkEvPrice](finance/BrkEvPrice.md) each take the
other four and answer the one they are named for. The round trip is recorded
in [BrkEvQuant](finance/BrkEvQuant.md), which is handed this answer and returns the
hundred units it started from.

Break-even is where this answers zero (HP help), which no row here measures;
the name describes the family rather than this member.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BrkEvQuant](finance/BrkEvQuant.md) · [BrkEvCost](finance/BrkEvCost.md) ·
[BrkEvFixed](finance/BrkEvFixed.md) · [BrkEvPrice](finance/BrkEvPrice.md)

---

<a name="BrkEvQuant"></a>

## BrkEvQuant

The quantity, from fixed cost, cost, price and profit.

| | |
|---|---|
| Syntax | `BrkEvQuant(fixed_cost,cost,price,profit)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("BrkEvQuant(1000,5,20,500)")` | `100` | [emulator](results.tsv) |
| `EXPR("BrkEvQuant(1000,5,20,BrkEvProfit(1000,100,5,20))")` | `100` | [emulator](results.tsv) |

### Behaviour

`BrkEvQuant(1000,5,20,500)` answers 100 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**The second row is the check that matters** (emulator). It hands this
command exactly what [BrkEvProfit](finance/BrkEvProfit.md) answered for a hundred
units, and gets a hundred back, exactly rather than to a few figures. That is
the calculator agreeing with itself across two names, and it is what says the
five `BrkEv` commands invert one equation rather than computing five
unrelated things.

**The exactness is worth noticing** (emulator). The `Tvm` family's round trip
comes back two billionths away from where it started, because it solves for a
rate by iteration; this one is a straight rearrangement, so it returns the
whole number.

The arguments omit the quantity, which is what this one answers (HP help), so
the positions differ from its siblings' and a program cannot reuse one
argument list across the family.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BrkEvProfit](finance/BrkEvProfit.md) · [BrkEvCost](finance/BrkEvCost.md) ·
[BrkEvFixed](finance/BrkEvFixed.md) · [BrkEvPrice](finance/BrkEvPrice.md)

---

<a name="CFData"></a>

## CFData

The cash flows, an empty list until a program puts some in.

| | |
|---|---|
| Syntax | `CFData` → list |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CFData")` | `{}` | [emulator](results.tsv) |

### Behaviour

**It answered `{}` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the cash flows the app analyses** (unverified): HP’s list gives this name and its app,
and nothing more.

**Empty, with every cash-flow result refusing** (emulator). [NPV](finance/NPV.md),
[IRR](finance/IRR.md) and six more refused under both app conditions, and a list with
nothing in it is the likeliest reason: there is nothing to discount. Filling
this and reading `NPV` again is the probe (unverified).

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CFPYR](finance/CFPYR.md) · [TotalCF](finance/TotalCF.md) · [NPV](finance/NPV.md)

---

<a name="CFPYR"></a>

## CFPYR

Cash flows per year, 12 as the app arrives.

| | |
|---|---|
| Syntax | `CFPYR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CFPYR")` | `12` | [emulator](results.tsv) |
| `EXPR(" CFPYR")` | `12.00` | [emulator](results.tsv) |

### Behaviour

**It answered `12` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how many cash flows fall in a year** (unverified): HP’s list gives this name and its app,
and nothing more.

**With the Finance app active it reads `12.00`, the same value with two decimals**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals). The decimals are the app's display, not part of the
value.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CFData](finance/CFData.md) · [PPYR](finance/PPYR.md) · [IRR](finance/IRR.md)

---

<a name="CPYR"></a>

## CPYR

Compounding periods per year, 12 once the Finance app is active.

| | |
|---|---|
| Syntax | `CPYR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CPYR")` | *error* | [emulator](results.tsv) |
| `EXPR(" CPYR")` | `12.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), [IPYR](finance/IPYR.md), [PV](finance/PV.md), [PMT](finance/PMT.md), [FV](finance/FV.md), [PPYR](finance/PPYR.md), `CPYR`, [BEG](finance/BEG.md) and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how many times a year interest compounds** (unverified): HP's list gives this name and its app, and
nothing more. It read `12.00` once Finance was active, a default the app arrives with.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PV](finance/PV.md) · [PPYR](finance/PPYR.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="CallPrice"></a>

## CallPrice

A bond’s call price, 100 as the app arrives.

| | |
|---|---|
| Syntax | `CallPrice` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CallPrice")` | `100` | [emulator](results.tsv) |

### Behaviour

**It answered `100` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the price at which a bond is called, per 100 of face value** (unverified): HP’s list gives this name and its app,
and nothing more.

**100 is a default, not a measurement** (emulator): nothing had been assigned.
It is the usual par for a price quoted per 100 of face value, which fits the
name (unverified).

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PriceBond](finance/PriceBond.md) · [YieldBond](finance/YieldBond.md) · [MatDate](finance/MatDate.md)

---

<a name="CashFlowFMRR"></a>

## CashFlowFMRR

A break-even rate of the same family, which this example cannot tell from MIRR.

| | |
|---|---|
| Syntax | `CashFlowFMRR(cash_flow_data, investment_rate, safe_investment_rate, [cashflows_per_year])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowFMRR({-1000,300,400,500},10,5)")` | `9.22317710801` | [emulator](results.tsv) |

### Behaviour

`CashFlowFMRR({-1000,300,400,500},10,5)` answers 9.22317710801 (emulator), a
plain real of type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**[CashFlowMIRR](finance/CashFlowMIRR.md) answered the same number, character for
character** (emulator), from the same list and the same two rates. One
example cannot separate the two commands, and this entry says so rather than
describing a difference nobody here has measured.

**The probe is a list where the rates have something to do** (emulator). The
flows used here go out once and come back three times, so the safe rate has
nothing to apply to. A list that turns negative again part way through is the
case the two names are said to treat differently, and it is one row away.

The same happened with two unit commands that agreed on the pair
they were given (emulator), and the answer then was the same: record the
agreement, name the probe, and do not invent the difference.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowMIRR](finance/CashFlowMIRR.md) · [CashFlowIRR](finance/CashFlowIRR.md)

---

<a name="CashFlowIRR"></a>

## CashFlowIRR

The rate at which a list of cash flows breaks even.

| | |
|---|---|
| Syntax | `CashFlowIRR(cash_flow_data,[cashflows_per_year])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowIRR({-1000,300,400,500})")` | `8.89633946933` | [emulator](results.tsv) |

### Behaviour

`CashFlowIRR({-1000,300,400,500})` answers 8.89633946933 (emulator), a plain
real of type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It is the rate that would make [CashFlowNPV](finance/CashFlowNPV.md) zero**
(emulator), and the two rows check each other: at ten per cent that command
answers −21.0368144252, a negative number, so the break-even rate has to lie
below ten. It does, at 8.8963.

**The answer is a percentage, like the rate the others take** (emulator),
which is what makes the pair comparable at all.

**It takes no rate, because the rate is what it answers** (HP help). The
optional second argument is how many flows fall in a year and was not run
(unverified).

A list of flows that never changes sign has no break-even rate, and what this
command does with one was not measured (unverified). That is the case worth
guarding, since it is the one where an answer would be meaningless rather
than merely surprising.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNPV](finance/CashFlowNPV.md) · [CashFlowMIRR](finance/CashFlowMIRR.md) ·
[CashFlowPB](finance/CashFlowPB.md)

---

<a name="CashFlowMIRR"></a>

## CashFlowMIRR

A break-even rate that separates the borrowing rate from the reinvestment one.

| | |
|---|---|
| Syntax | `CashFlowMIRR(cash_flow_data, investment_rate, safe_investment_rate, [cashflows_per_year])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowMIRR({-1000,300,400,500},10,5)")` | `9.22317710801` | [emulator](results.tsv) |

### Behaviour

`CashFlowMIRR({-1000,300,400,500},10,5)` answers 9.22317710801 (emulator), a
plain real of type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It sits above the plain break-even rate** (emulator).
[CashFlowIRR](finance/CashFlowIRR.md) answers 8.89633946933 for the same list, and
the difference is what the two extra rates buy: the returns here are taken as
reinvested at ten per cent rather than at the answer itself.

**[CashFlowFMRR](finance/CashFlowFMRR.md) answered exactly the same number, to every
figure** (emulator). The two commands cannot be told apart by this row, so
this entry does not describe a difference it has not seen. Separating them
wants a list where the rates matter more -- a flow that turns negative again
after the outlay, which is the case the two are said to treat differently.

The two rates go in investment first, safe second (HP help), and nothing
measured here confirms which the answer leaned on.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowFMRR](finance/CashFlowFMRR.md) · [CashFlowIRR](finance/CashFlowIRR.md) ·
[CashFlowNPV](finance/CashFlowNPV.md)

---

<a name="CashFlowNFV"></a>

## CashFlowNFV

What a list of cash flows is worth at the end, at a given rate.

| | |
|---|---|
| Syntax | `CashFlowNFV(cash_flow_data, investment_rate, [cashflows_per_year])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowNFV({-1000,300,400,500},10)")` | `−28` | [emulator](results.tsv) |

### Behaviour

`CashFlowNFV({-1000,300,400,500},10)` answers −28 (emulator), a plain real of
type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It is the present value carried forward, and the arithmetic is exact**
(emulator). [CashFlowNPV](finance/CashFlowNPV.md) answers −21.0368144252 for the same
list and rate, and that times 1.1 cubed is −28 to every figure the calculator
printed. Three periods of growth, one for each flow after the first, which is
what says the horizon is the last flow rather than the count of them.

**A round number out of an untidy one is worth trusting rather than
suspecting** (emulator): it falls out of the list chosen here and is not a
sign that the command rounds.

**The minus sign is the calculator's own, U+2212** (emulator):
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign). The Result cell was
built from the stored row rather than typed.

The third argument sets how many flows fall in a year (HP help) and was not
run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNPV](finance/CashFlowNPV.md) · [CashFlowNUS](finance/CashFlowNUS.md) ·
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign)

---

<a name="CashFlowNPV"></a>

## CashFlowNPV

What a list of cash flows is worth today, at a given rate.

| | |
|---|---|
| Syntax | `CashFlowNPV(cash_flow_data, investment_rate, [cashflows_per_year])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowNPV({-1000,300,400,500},10)")` | `−21.0368144252` | [emulator](results.tsv) |

### Behaviour

`CashFlowNPV({-1000,300,400,500},10)` answers −21.0368144252 (emulator), a
plain real of type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**The answer says how the arguments are read** (emulator). Three hundred over
1.1, plus four hundred over 1.21, plus five hundred over 1.331, less the
thousand, is −21.04. So the rate is a percentage rather than a fraction, the
first flow sits at time zero and is not discounted, and each later one is
discounted by one more period.

**A negative answer means the flows do not clear the rate asked for**
(emulator), and [CashFlowIRR](finance/CashFlowIRR.md) says by how much: it answers
8.89633946933, which is below the ten per cent demanded here. The two rows
agree, and a program can use one to sanity-check the other.

**The minus sign is the calculator's own, U+2212** (emulator), not the hyphen
a keyboard types:
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign). The Result cell above
was built from the stored row rather than typed.

The third argument sets how many flows fall in a year (HP help) and was not
run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowIRR](finance/CashFlowIRR.md) · [CashFlowNFV](finance/CashFlowNFV.md) ·
[CashFlowTotal](finance/CashFlowTotal.md) ·
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign)

---

<a name="CashFlowNUS"></a>

## CashFlowNUS

The level amount per period worth the same as the flows.

| | |
|---|---|
| Syntax | `CashFlowNUS(cash_flow_data, investment_rate, [cashflows_per_year])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowNUS({-1000,300,400,500},10)")` | `−8.45921450151` | [emulator](results.tsv) |

### Behaviour

`CashFlowNUS({-1000,300,400,500},10)` answers −8.45921450151 (emulator), a
plain real of type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It is the same value as the other two, said as a series** (emulator).
[CashFlowNPV](finance/CashFlowNPV.md) answers −21.0368144252 today and
[CashFlowNFV](finance/CashFlowNFV.md) answers −28 at the end; spreading that present
value evenly over three periods at ten per cent gives −8.459. Three ways of
writing one number, which is why a program should pick the one its reader
expects rather than computing all three.

**The spread is over three periods, not four** (emulator), matching the
horizon [CashFlowNFV](finance/CashFlowNFV.md) shows: the flow at time zero is the
outlay, not one of the periods.

**The minus sign is the calculator's own, U+2212** (emulator):
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign). The Result cell was
built from the stored row rather than typed.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNPV](finance/CashFlowNPV.md) · [CashFlowNFV](finance/CashFlowNFV.md) ·
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign)

---

<a name="CashFlowPB"></a>

## CashFlowPB

How many periods it takes to get the money back.

| | |
|---|---|
| Syntax | `CashFlowPB(cash_flow_data, [investment_rate])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowPB({-1000,300,400,500})")` | `2.6` | [emulator](results.tsv) |

### Behaviour

`CashFlowPB({-1000,300,400,500})` answers 2.6 (emulator), a plain real of
type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**The answer shows the flows are counted undiscounted here** (emulator).
Three hundred and four hundred recover seven hundred of the thousand in two
periods, and three hundred of the third period's five hundred finishes it:
two periods plus three fifths, which is the 2.6 that came back. No
discounting is applied, because the rate was not given.

**It answers a fraction of a period, not a whole one** (emulator). The money
does not arrive part way through in most real schedules, so a caller that
needs a whole number has to round, and rounding down would claim the money
back before it is.

The rate is optional (HP help) and was not run (unverified). What it does to
the answer -- whether the recovery is then measured against discounted flows
-- is the probe worth one row.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowTotal](finance/CashFlowTotal.md) · [CashFlowNPV](finance/CashFlowNPV.md) ·
[CashFlowIRR](finance/CashFlowIRR.md)

---

<a name="CashFlowTotal"></a>

## CashFlowTotal

Adds up a list of cash flows, without discounting them.

| | |
|---|---|
| Syntax | `CashFlowTotal(cash_flow_data)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CashFlowTotal({-1000,300,400,500})")` | `200` | [emulator](results.tsv) |

### Behaviour

`CashFlowTotal({-1000,300,400,500})` answers 200 (emulator), a plain real of
type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes): minus a
thousand plus three hundred plus four hundred plus five hundred.

**It is the only one of the family that takes no rate, and that is the
point** (emulator). Every other `CashFlow` command discounts, so it answers
what the flows are worth at some moment; this one answers what they add up
to, which is a different question and usually a larger number.
[CashFlowNPV](finance/CashFlowNPV.md) gives −21.0368144252 for this same list at ten
per cent.

The flows go in as a list and the first is the one at time zero (emulator),
which is what the discounted members of the family show by leaving it
undiscounted.

What it does with an empty list was not run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNPV](finance/CashFlowNPV.md) · [CashFlowIRR](finance/CashFlowIRR.md) ·
[CashFlowPB](finance/CashFlowPB.md)

---

<a name="Change"></a>

## Change

The percentage change between two values.

| | |
|---|---|
| Syntax | `Change` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Change")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the percentage change from an old value to a new one** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[OldValue](finance/OldValue.md) · [NewValue](finance/NewValue.md) · [Total](finance/Total.md)

---

<a name="ChangeCost"></a>

## ChangeCost

The cost that a price and a percentage give.

| | |
|---|---|
| Syntax | `ChangeCost(price,percentage,option)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("ChangeCost(100,10,0)")` | `90.9090909091` | [emulator](results.tsv) |

### Behaviour

`ChangeCost(100,10,0)` answers 90.9090909091 (emulator), a plain real of type
0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes): a hundred divided by
1.1.

**It divides where [ChangePrice](finance/ChangePrice.md) multiplies** (emulator). The
two are inverses at the same percentage, so a cost sent through one and back
through the other returns where it started.

**It is not a hundred less ten per cent** (emulator). That would be ninety,
and a program that subtracts instead of dividing is out by about one per cent
here and by more as the percentage grows. The difference is small enough to
pass a glance and large enough to matter.

The third argument is called an option and was 0 here (emulator). What
another value does was not run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[ChangePrice](finance/ChangePrice.md) · [ChangeNew](finance/ChangeNew.md) ·
[ChangeOld](finance/ChangeOld.md)

---

<a name="ChangeNew"></a>

## ChangeNew

Answers something an ordinary reading of its arguments does not explain.

| | |
|---|---|
| Syntax | `ChangeNew(old,percentage,option)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("ChangeNew(100,10,0)")` | `10` | [emulator](results.tsv) |

### Behaviour

`ChangeNew(100,10,0)` answers 10 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**That is not a hundred raised by ten per cent, which would be 110**
(emulator). Ten is a hundred *times* ten per cent, so what came back looks
like the size of the change rather than the value after it -- but this entry
does not state that as the rule, because one row cannot tell that reading
from several others.

**Its sibling behaves the same way** (emulator): [ChangeOld](finance/ChangeOld.md)
answers 1100 for 110 and ten per cent, which is 110 times ten, not 110
reduced to its earlier value. Two of this family read as multiplications by
the percentage while the other two,
[ChangePrice](finance/ChangePrice.md) and [ChangeCost](finance/ChangeCost.md), read as a
proper rise and fall.

**The third argument is the likeliest explanation and it is untested**
(unverified). HP calls it an option and publishes no values for it; 0 was
used here because a probe has to pick something. The probe is the same call
with 1 and with 2, which is two rows and would either explain these answers
or rule the option out.

Until that runs, a program should not use this name expecting the value after
a change (unverified). [ChangePrice](finance/ChangePrice.md) is measured and does
that.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[ChangeOld](finance/ChangeOld.md) · [ChangePrice](finance/ChangePrice.md) ·
[ChangeCost](finance/ChangeCost.md)

---

<a name="ChangeOld"></a>

## ChangeOld

Answers something an ordinary reading of its arguments does not explain.

| | |
|---|---|
| Syntax | `ChangeOld(new,percentage,option)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("ChangeOld(110,10,0)")` | `1100` | [emulator](results.tsv) |

### Behaviour

`ChangeOld(110,10,0)` answers 1100 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**That is not the value before a ten per cent rise, which would be 100**
(emulator). It is 110 times ten, so the percentage appears to have been used
as a plain multiplier rather than as a percentage at all.

**[ChangeNew](finance/ChangeNew.md) behaves the same way** and carries the full
account of what is and is not known about this family (emulator), including
the probe that would settle it: the same call with other values of the third
argument, which HP calls an option and documents no values for.

A program that needs the value before a rise has
[ChangeCost](finance/ChangeCost.md), which divides and is measured (emulator).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[ChangeNew](finance/ChangeNew.md) · [ChangeCost](finance/ChangeCost.md) ·
[ChangePrice](finance/ChangePrice.md)

---

<a name="ChangePrice"></a>

## ChangePrice

The price that a cost and a percentage give.

| | |
|---|---|
| Syntax | `ChangePrice(cost,percentage,option)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("ChangePrice(60,10,0)")` | `66` | [emulator](results.tsv) |

### Behaviour

`ChangePrice(60,10,0)` answers 66 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes): sixty raised by ten per
cent.

**[ChangeCost](finance/ChangeCost.md) undoes it** (emulator). Given a hundred and the
same ten per cent it answers 90.9090909091, which is a hundred divided by
1.1, so the pair go up and down the same step.

**The third argument is called an option and nothing here says what it
selects** (emulator). It was 0 in this row, and 0 gives the plain reading
above. What any other value does was not run (unverified), and it matters,
because two of the four names in this family answer something the same
reading does not explain -- see [ChangeNew](finance/ChangeNew.md).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[ChangeCost](finance/ChangeCost.md) · [ChangeNew](finance/ChangeNew.md) ·
[ChangeOld](finance/ChangeOld.md)

---

<a name="Cost"></a>

## Cost

The cost a margin or markup is taken on.

| | |
|---|---|
| Syntax | `Cost` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Cost")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the cost price of a margin or markup calculation** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[SalePrice](finance/SalePrice.md) · [Margin](finance/Margin.md) · [Markup](finance/Markup.md)

---

<a name="CostAsset"></a>

## CostAsset

The cost of an asset being depreciated.

| | |
|---|---|
| Syntax | `CostAsset` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CostAsset")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the cost of the asset a depreciation starts from** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[SalvageAsset](finance/SalvageAsset.md) · [LifeAsset](finance/LifeAsset.md) · [FactorDepr](finance/FactorDepr.md)

---

<a name="CpnPer"></a>

## CpnPer

A bond’s coupon, 0 on a fresh calculator.

| | |
|---|---|
| Syntax | `CpnPer` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("CpnPer")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the coupon a bond pays** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PriceBond](finance/PriceBond.md) · [SemiAnnual](finance/SemiAnnual.md) · [Accrued](finance/Accrued.md)

---

<a name="Date360"></a>

## Date360

Whether date arithmetic counts a 360-day year.

| | |
|---|---|
| Syntax | `Date360` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Date360")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds a switch between a 360-day and an actual year for dates** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[DateDiff](finance/DateDiff.md) · [Bond360](finance/Bond360.md) · [DateOne](finance/DateOne.md)

---

<a name="DateDays"></a>

## DateDays

The days between two dates, refused in the form tried here.

| | |
|---|---|
| Syntax | `DateDays(first_date,second_date,[cal_360])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("DateDays(1.012024,12.312024)")` | *error* | [emulator](results.tsv) |

### Behaviour

**The call is refused** (emulator). The two arguments were written in the
shape the Prime uses to show a date, a month before the point and a day and
year after it, and the answer was an error rather than a count of days.

**Two more commands that take dates were refused in the same batch**
(emulator): [BondPrice](finance/BondPrice.md) and [BondYield](finance/BondYield.md), each
with dates written the same way. Three refusals sharing one argument shape is
better evidence about the shape than about the three commands.

**So the likeliest cause is the date, not the command** (unverified). Nothing
here measures what the calculator will accept: a different number of digits
in the year, a date built by the calculator itself rather than typed, or a
date held in one of the app's own variables are all untried.

The probe is one call whose date comes from the machine rather than from these
pages (unverified), which separates a bad format from a command that needs
something else entirely.

The third argument chooses a 360-day calendar (HP help) and was not reached.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BondPrice](finance/BondPrice.md) · [BondYield](finance/BondYield.md)

---

<a name="DateDiff"></a>

## DateDiff

The days between two dates, 0 while both are the same.

| | |
|---|---|
| Syntax | `DateDiff` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("DateDiff")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the days between two dates** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[DateOne](finance/DateOne.md) · [DateTwo](finance/DateTwo.md) · [Date360](finance/Date360.md)

---

<a name="DateOne"></a>

## DateOne

The first date of a date calculation, 2017.0101 as the app arrives.

| | |
|---|---|
| Syntax | `DateOne` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("DateOne")` | `2017.0101` | [emulator](results.tsv) |

### Behaviour

**It answered `2017.0101` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the first of two dates** (unverified): HP’s list gives this name and its app,
and nothing more.

**2017.0101 is a default, and all five date variables of this app arrived
holding it** (emulator). Read in the YYYY.MMDD form HP's date values take, it
is 1 January 2017, which is untested here (unverified). A program reading it
as a plain number gets 2017.0101.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[DateTwo](finance/DateTwo.md) · [DateDiff](finance/DateDiff.md) · [SetDate](finance/SetDate.md)

---

<a name="DateTwo"></a>

## DateTwo

The second date of a date calculation, the same 2017.0101.

| | |
|---|---|
| Syntax | `DateTwo` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("DateTwo")` | `2017.0101` | [emulator](results.tsv) |

### Behaviour

**It answered `2017.0101` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the second of two dates** (unverified): HP’s list gives this name and its app,
and nothing more.

**2017.0101 is a default, and all five date variables of this app arrived
holding it** (emulator). Read in the YYYY.MMDD form HP's date values take, it
is 1 January 2017, which is untested here (unverified). A program reading it
as a plain number gets 2017.0101.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[DateOne](finance/DateOne.md) · [DateDiff](finance/DateDiff.md) · [MatDate](finance/MatDate.md)

---

<a name="Depreciate"></a>

## Depreciate

Depreciation over the life of an asset, refused in the form tried here.

| | |
|---|---|
| Syntax | `Depreciate(method, cost, salvage, life, [first], [factor])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Depreciate(\"SL\",1000,100,5)")` | *error* | [emulator](results.tsv) |

### Behaviour

**The call is refused** (emulator). The method was sent as the string `"SL"`,
for straight line, with a cost of a thousand, a salvage of a hundred and a
life of five.

**The method is the argument nothing is known about** (emulator). HP's list
names it and gives no values for it (HP help), so a string was chosen because
a method has to be named somehow. The refusal says that choice was wrong, or
that something else in the call was, and it does not say which.

**It is the only finance command refused that takes no date** (emulator),
which separates it from [DateDays](finance/DateDays.md),
[BondPrice](finance/BondPrice.md) and [BondYield](finance/BondYield.md), whose shared suspect
is the date shape. Whatever is wrong here is its own.

The probes worth one row each (unverified): the method as a bare name rather
than a string, and the method as a number, which is how several HP apps
select a mode.

The last two arguments are optional (HP help) and were not reached.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BondPrice](finance/BondPrice.md) · [CashFlowTotal](finance/CashFlowTotal.md)

---

<a name="DiscPayback"></a>

## DiscPayback

The discounted payback period, refused either way.

| | |
|---|---|
| Syntax | `DiscPayback` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("DiscPayback")` | *error* | [emulator](results.tsv) |
| `EXPR(" DiscPayback")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the discounted payback period of the cash flows** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="Dividend"></a>

## Dividend

The dividend yield in a Black-Scholes calculation.

| | |
|---|---|
| Syntax | `Dividend` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Dividend")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the dividend yield a Black-Scholes value allows for** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BSCall](finance/BSCall.md) · [RiskFree](finance/RiskFree.md) · [StockPrice](finance/StockPrice.md)

---

<a name="EffInt"></a>

## EffInt

An effective interest rate.

| | |
|---|---|
| Syntax | `EffInt` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("EffInt")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the effective rate an interest conversion produces or starts from** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[NomInt](finance/NomInt.md) · [IntCPYR](finance/IntCPYR.md) · [IPYR](finance/IPYR.md)

---

<a name="FMRR"></a>

## FMRR

The financial management rate of return, refused either way.

| | |
|---|---|
| Syntax | `FMRR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("FMRR")` | *error* | [emulator](results.tsv) |
| `EXPR(" FMRR")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the financial management rate of return** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

**Its twin function answers, given the flows as arguments** (emulator):
[CashFlowFMRR](finance/CashFlowFMRR.md) was measured and returned a value. A program
wanting this number does not need the app at all.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowFMRR](finance/CashFlowFMRR.md) · [CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="FV"></a>

## FV

The future value, which needs the Finance app to be read.

| | |
|---|---|
| Syntax | `FV` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("FV")` | *error* | [emulator](results.tsv) |
| `EXPR(" FV")` | `0.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), [IPYR](finance/IPYR.md), [PV](finance/PV.md), [PMT](finance/PMT.md), `FV`, [PPYR](finance/PPYR.md), [CPYR](finance/CPYR.md), [BEG](finance/BEG.md) and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the future value** (unverified): HP's list gives this name and its app, and
nothing more. It read `0.00` once Finance was active.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Its twin function answers without the app** (emulator):
[TvmFV](finance/TvmFV.md) was measured from a batch nobody had touched, so
with the Function app active, and it answered. A program that cannot select
the Finance app can still compute this value by calling the function with
its arguments.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmFV](finance/TvmFV.md) · [PV](finance/PV.md) · [PPYR](finance/PPYR.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="FactorDepr"></a>

## FactorDepr

The declining-balance factor, 200 as the app arrives.

| | |
|---|---|
| Syntax | `FactorDepr` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("FactorDepr")` | `200` | [emulator](results.tsv) |

### Behaviour

**It answered `200` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the factor of a declining-balance depreciation, as a percentage** (unverified): HP’s list gives this name and its app,
and nothing more.

**200 is a default** (emulator), and 200 per cent is the usual double
declining balance, which fits the name (unverified).

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CostAsset](finance/CostAsset.md) · [LifeAsset](finance/LifeAsset.md) · [FirstAsset](finance/FirstAsset.md)

---

<a name="FinType"></a>

## FinType

A code for which finance calculation the app is set to.

| | |
|---|---|
| Syntax | `FinType` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("FinType")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds a code selecting a calculation** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[BEG](finance/BEG.md) · [CFData](finance/CFData.md) · [PV](finance/PV.md)

---

<a name="FirstAsset"></a>

## FirstAsset

The first year of a depreciation, 1 as the app arrives.

| | |
|---|---|
| Syntax | `FirstAsset` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("FirstAsset")` | `1` | [emulator](results.tsv) |

### Behaviour

**It answered `1` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds which year of an asset’s life a depreciation is taken for** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[LifeAsset](finance/LifeAsset.md) · [FirstDateAsset](finance/FirstDateAsset.md) · [CostAsset](finance/CostAsset.md)

---

<a name="FirstDateAsset"></a>

## FirstDateAsset

The date a depreciation starts, 2017.0101 as the app arrives.

| | |
|---|---|
| Syntax | `FirstDateAsset` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("FirstDateAsset")` | `2017.0101` | [emulator](results.tsv) |

### Behaviour

**It answered `2017.0101` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the date an asset starts depreciating** (unverified): HP’s list gives this name and its app,
and nothing more.

**2017.0101 is a default, and all five date variables of this app arrived
holding it** (emulator). Read in the YYYY.MMDD form HP's date values take, it
is 1 January 2017, which is untested here (unverified). A program reading it
as a plain number gets 2017.0101.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[FirstAsset](finance/FirstAsset.md) · [LifeAsset](finance/LifeAsset.md) · [DateOne](finance/DateOne.md)

---

<a name="FixedCost"></a>

## FixedCost

The fixed cost of a break-even calculation.

| | |
|---|---|
| Syntax | `FixedCost` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("FixedCost")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the fixed cost of a break-even** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[VariableCost](finance/VariableCost.md) · [Price](finance/Price.md) · [Profit](finance/Profit.md)

---

<a name="GSize"></a>

## GSize

The group size of an amortisation, 12 once Finance is active.

| | |
|---|---|
| Syntax | `GSize` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("GSize")` | *error* | [emulator](results.tsv) |
| `EXPR(" GSize")` | `12.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), [IPYR](finance/IPYR.md), [PV](finance/PV.md), [PMT](finance/PMT.md), [FV](finance/FV.md), [PPYR](finance/PPYR.md), [CPYR](finance/CPYR.md), [BEG](finance/BEG.md) and `GSize`, the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how many payments an amortisation groups together** (unverified): HP's list gives this name and its app, and
nothing more. It read `12.00` once Finance was active, a default the app arrives with.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PV](finance/PV.md) · [PPYR](finance/PPYR.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="IPYR"></a>

## IPYR

The interest rate per year, which needs the Finance app to be read.

| | |
|---|---|
| Syntax | `IPYR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("IPYR")` | *error* | [emulator](results.tsv) |
| `EXPR(" IPYR")` | `0.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), `IPYR`, [PV](finance/PV.md), [PMT](finance/PMT.md), [FV](finance/FV.md), [PPYR](finance/PPYR.md), [CPYR](finance/CPYR.md), [BEG](finance/BEG.md) and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the interest rate per year** (unverified): HP's list gives this name and its app, and
nothing more. It read `0.00` once Finance was active.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Its twin function answers without the app** (emulator):
[TvmIPYR](finance/TvmIPYR.md) was measured from a batch nobody had touched, so
with the Function app active, and it answered. A program that cannot select
the Finance app can still compute this value by calling the function with
its arguments.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmIPYR](finance/TvmIPYR.md) · [PV](finance/PV.md) · [PPYR](finance/PPYR.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="IRR"></a>

## IRR

The internal rate of return, refused with or without the Finance app.

| | |
|---|---|
| Syntax | `IRR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("IRR")` | *error* | [emulator](results.tsv) |
| `EXPR(" IRR")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the internal rate of return of the cash flows** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

**Its twin function answers, given the flows as arguments** (emulator):
[CashFlowIRR](finance/CashFlowIRR.md) was measured and returned a value. A program
wanting this number does not need the app at all.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowIRR](finance/CashFlowIRR.md) · [CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="IntCPYR"></a>

## IntCPYR

Compounding periods per year for interest conversion, 12 as the app arrives.

| | |
|---|---|
| Syntax | `IntCPYR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("IntCPYR")` | `12` | [emulator](results.tsv) |

### Behaviour

**It answered `12` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how many times a year interest compounds when converting rates** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[NomInt](finance/NomInt.md) · [EffInt](finance/EffInt.md) · [CPYR](finance/CPYR.md)

---

<a name="IntConvCPYR"></a>

## IntConvCPYR

How often a rate compounds, from the nominal and effective rates.

| | |
|---|---|
| Syntax | `IntConvCPYR(nominal_rate,effective_rate)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("IntConvCPYR(12,12.6825)")` | `11.9994580668` | [emulator](results.tsv) |

### Behaviour

`IntConvCPYR(12,12.6825)` answers 11.9994580668 (emulator), a plain real of
type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It recovers the twelve compoundings the pair was built from** (emulator),
short by half a thousandth for the same reason
[IntConvNom](finance/IntConvNom.md) is: the effective rate was handed over rounded to
12.6825 rather than 12.6825030132.

**It answers a fraction where the quantity is a count** (emulator). Nothing
compounds 11.9994580668 times a year, so a caller either rounds or reads the
answer as evidence that the two rates do not quite belong to a whole number
of periods. Which the calculator intends is not measured here (unverified).

The nominal rate comes first and the effective second (HP help), which is the
order the row above used.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[IntConvEff](finance/IntConvEff.md) · [IntConvNom](finance/IntConvNom.md)

---

<a name="IntConvEff"></a>

## IntConvEff

The effective rate, from a nominal rate and how often it compounds.

| | |
|---|---|
| Syntax | `IntConvEff(nominal_rate,compounds_per_year)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("IntConvEff(12,12)")` | `12.6825030132` | [emulator](results.tsv) |

### Behaviour

`IntConvEff(12,12)` answers 12.6825030132 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**The answer says the arithmetic** (emulator): one plus twelve over twelve
hundred, raised to the twelfth, less one, is 0.126825, and twelve per cent
compounded monthly is worth 12.6825 per cent over the year. So both arguments
are read as percentages and the answer is a percentage too, not a fraction.

**The two arguments are easy to swap and the mistake is quiet** (emulator).
Both are 12 in the row above, which is why this entry says the order rather
than relying on the example: the rate comes first and the number of
compoundings second.

[IntConvNom](finance/IntConvNom.md) reverses it and [IntConvCPYR](finance/IntConvCPYR.md)
answers the third quantity, so the three together cover one relation
(emulator): each was given what the others answered and returned the value
that started the chain, short only by the figures dropped in writing it down.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[IntConvNom](finance/IntConvNom.md) · [IntConvCPYR](finance/IntConvCPYR.md) ·
[TvmIPYR](finance/TvmIPYR.md)

---

<a name="IntConvNom"></a>

## IntConvNom

The nominal rate, from an effective rate and how often it compounds.

| | |
|---|---|
| Syntax | `IntConvNom(effective_rate,compounds_per_year)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("IntConvNom(12.6825,12)")` | `11.9999972992` | [emulator](results.tsv) |

### Behaviour

`IntConvNom(12.6825,12)` answers 11.9999972992 (emulator), a plain real of
type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It recovers the twelve per cent [IntConvEff](finance/IntConvEff.md) started from**
(emulator), and the three millionths missing are the four figures dropped
when 12.6825030132 was written down as 12.6825, not a disagreement between
the two commands.

That makes the pair a round trip in the same sense as the `Tvm` and `BrkEv`
families (emulator): each undoes the other, so a program can check its own
handling by going out and back.

The effective rate comes first, the compoundings second (HP help), the same
order [IntConvEff](finance/IntConvEff.md) takes with the nominal rate in front.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[IntConvEff](finance/IntConvEff.md) · [IntConvCPYR](finance/IntConvCPYR.md)

---

<a name="InvestInt"></a>

## InvestInt

The reinvestment rate a modified return uses.

| | |
|---|---|
| Syntax | `InvestInt` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("InvestInt")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the rate cash is assumed to be reinvested at** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[SafeInt](finance/SafeInt.md) · [MIRR](finance/MIRR.md) · [FMRR](finance/FMRR.md)

---

<a name="LifeAsset"></a>

## LifeAsset

The life of an asset in years, 1 as the app arrives.

| | |
|---|---|
| Syntax | `LifeAsset` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("LifeAsset")` | `1` | [emulator](results.tsv) |

### Behaviour

**It answered `1` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how many years an asset depreciates over** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CostAsset](finance/CostAsset.md) · [FirstAsset](finance/FirstAsset.md) · [SalvageAsset](finance/SalvageAsset.md)

---

<a name="MIRR"></a>

## MIRR

The modified internal rate of return, refused either way.

| | |
|---|---|
| Syntax | `MIRR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("MIRR")` | *error* | [emulator](results.tsv) |
| `EXPR(" MIRR")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the modified internal rate of return** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

**Its twin function answers, given the flows as arguments** (emulator):
[CashFlowMIRR](finance/CashFlowMIRR.md) was measured and returned a value. A program
wanting this number does not need the app at all.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowMIRR](finance/CashFlowMIRR.md) · [CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="Macaulay"></a>

## Macaulay

A bond’s Macaulay duration.

| | |
|---|---|
| Syntax | `Macaulay` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Macaulay")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the Macaulay duration of a bond** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Modified](finance/Modified.md) · [YieldBond](finance/YieldBond.md) · [PriceBond](finance/PriceBond.md)

---

<a name="Margin"></a>

## Margin

A margin as a percentage of the sale price.

| | |
|---|---|
| Syntax | `Margin` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Margin")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the margin, as a percentage of the sale price** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Markup](finance/Markup.md) · [Cost](finance/Cost.md) · [SalePrice](finance/SalePrice.md)

---

<a name="Markup"></a>

## Markup

A markup as a percentage of the cost.

| | |
|---|---|
| Syntax | `Markup` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Markup")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the markup, as a percentage of the cost** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Margin](finance/Margin.md) · [Cost](finance/Cost.md) · [SalePrice](finance/SalePrice.md)

---

<a name="MatDate"></a>

## MatDate

A bond’s maturity date, 2017.0101 as the app arrives.

| | |
|---|---|
| Syntax | `MatDate` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("MatDate")` | `2017.0101` | [emulator](results.tsv) |

### Behaviour

**It answered `2017.0101` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the date a bond matures** (unverified): HP’s list gives this name and its app,
and nothing more.

**2017.0101 is a default, and all five date variables of this app arrived
holding it** (emulator). Read in the YYYY.MMDD form HP's date values take, it
is 1 January 2017, which is untested here (unverified). A program reading it
as a plain number gets 2017.0101.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[SetDate](finance/SetDate.md) · [PriceBond](finance/PriceBond.md) · [CallPrice](finance/CallPrice.md)

---

<a name="Modified"></a>

## Modified

A bond’s modified duration.

| | |
|---|---|
| Syntax | `Modified` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Modified")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the modified duration of a bond** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Macaulay](finance/Macaulay.md) · [YieldBond](finance/YieldBond.md) · [PriceBond](finance/PriceBond.md)

---

<a name="NFV"></a>

## NFV

The net future value, refused with or without the Finance app.

| | |
|---|---|
| Syntax | `NFV` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("NFV")` | *error* | [emulator](results.tsv) |
| `EXPR(" NFV")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the net future value of the cash flows** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

**Its twin function answers, given the flows as arguments** (emulator):
[CashFlowNFV](finance/CashFlowNFV.md) was measured and returned a value. A program
wanting this number does not need the app at all.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNFV](finance/CashFlowNFV.md) · [CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="NPV"></a>

## NPV

The net present value, refused with or without the Finance app.

| | |
|---|---|
| Syntax | `NPV` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("NPV")` | *error* | [emulator](results.tsv) |
| `EXPR(" NPV")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the net present value of the cash flows** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

**Its twin function answers, given the flows as arguments** (emulator):
[CashFlowNPV](finance/CashFlowNPV.md) was measured and returned a value. A program
wanting this number does not need the app at all.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNPV](finance/CashFlowNPV.md) · [CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="NUS"></a>

## NUS

The net uniform series, refused with or without the Finance app.

| | |
|---|---|
| Syntax | `NUS` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("NUS")` | *error* | [emulator](results.tsv) |
| `EXPR(" NUS")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the net uniform series of the cash flows** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

**Its twin function answers, given the flows as arguments** (emulator):
[CashFlowNUS](finance/CashFlowNUS.md) was measured and returned a value. A program
wanting this number does not need the app at all.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowNUS](finance/CashFlowNUS.md) · [CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="NbPmt"></a>

## NbPmt

The number of payments, which needs the Finance app to be read.

| | |
|---|---|
| Syntax | `NbPmt` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("NbPmt")` | *error* | [emulator](results.tsv) |
| `EXPR(" NbPmt")` | `0.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- `NbPmt`, [IPYR](finance/IPYR.md), [PV](finance/PV.md), [PMT](finance/PMT.md), [FV](finance/FV.md), [PPYR](finance/PPYR.md), [CPYR](finance/CPYR.md), [BEG](finance/BEG.md) and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the number of payments** (unverified): HP's list gives this name and its app, and
nothing more. It read `0.00` once Finance was active.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Its twin function answers without the app** (emulator):
[TvmNbPmt](finance/TvmNbPmt.md) was measured from a batch nobody had touched, so
with the Function app active, and it answered. A program that cannot select
the Finance app can still compute this value by calling the function with
its arguments.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmNbPmt](finance/TvmNbPmt.md) · [PV](finance/PV.md) · [PPYR](finance/PPYR.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="NewValue"></a>

## NewValue

The new value of a percentage change.

| | |
|---|---|
| Syntax | `NewValue` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("NewValue")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the value a percentage change ends at** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[OldValue](finance/OldValue.md) · [Change](finance/Change.md) · [Total](finance/Total.md)

---

<a name="NomInt"></a>

## NomInt

A nominal interest rate.

| | |
|---|---|
| Syntax | `NomInt` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("NomInt")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the nominal rate an interest conversion produces or starts from** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[EffInt](finance/EffInt.md) · [IntCPYR](finance/IntCPYR.md) · [IPYR](finance/IPYR.md)

---

<a name="OldValue"></a>

## OldValue

The old value of a percentage change.

| | |
|---|---|
| Syntax | `OldValue` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("OldValue")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the value a percentage change starts from** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[NewValue](finance/NewValue.md) · [Change](finance/Change.md) · [Total](finance/Total.md)

---

<a name="PMT"></a>

## PMT

The payment, which needs the Finance app to be read.

| | |
|---|---|
| Syntax | `PMT` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PMT")` | *error* | [emulator](results.tsv) |
| `EXPR(" PMT")` | *error* | [emulator](results.tsv) |
| `EXPR("  PMT")` | `0.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), [IPYR](finance/IPYR.md), [PV](finance/PV.md), `PMT`, [FV](finance/FV.md), [PPYR](finance/PPYR.md), [CPYR](finance/CPYR.md), [BEG](finance/BEG.md) and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the payment each period** (unverified): HP's list gives this name and its app, and
nothing more. It read `0.00` once Finance was active.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Its twin function answers without the app** (emulator):
[TvmPMT](finance/TvmPMT.md) was measured from a batch nobody had touched, so
with the Function app active, and it answered. A program that cannot select
the Finance app can still compute this value by calling the function with
its arguments.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmPMT](finance/TvmPMT.md) · [PV](finance/PV.md) · [PPYR](finance/PPYR.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="PPYR"></a>

## PPYR

Payments per year, 12 once the Finance app is active.

| | |
|---|---|
| Syntax | `PPYR` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PPYR")` | *error* | [emulator](results.tsv) |
| `EXPR(" PPYR")` | `12.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), [IPYR](finance/IPYR.md), [PV](finance/PV.md), [PMT](finance/PMT.md), [FV](finance/FV.md), `PPYR`, [CPYR](finance/CPYR.md), [BEG](finance/BEG.md) and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how many payments fall in a year** (unverified): HP's list gives this name and its app, and
nothing more. It read `12.00` once Finance was active, a default the app arrives with.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PV](finance/PV.md) · [BEG](finance/BEG.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="PV"></a>

## PV

The present value: needs the Finance app, and a program can set it.

| | |
|---|---|
| Syntax | `PV` → real |
| Syntax | `PV:=real` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PV")` | *error* | [emulator](results.tsv) |
| `EXPR(" PV")` | `0.00` | [emulator](results.tsv) |
| `EXPR("PV:=1000")` | `1000.00` | [emulator](results.tsv) |
| `EXPR("(PV)")` | `1000.00` | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and answered once Finance was
selected** (emulator), with nothing else changed between the two batches. It is
one of nine Finance variables that behave this way -- [NbPmt](finance/NbPmt.md), [IPYR](finance/IPYR.md), `PV`, [PMT](finance/PMT.md), [FV](finance/FV.md), [PPYR](finance/PPYR.md), [CPYR](finance/CPYR.md), [BEG](finance/BEG.md) and [GSize](finance/GSize.md), the
time-value-of-money names -- while 50 of their neighbours answer from any app, [apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the present value** (unverified): HP's list gives this name and its app, and
nothing more. It read `0.00` once Finance was active.

**The two decimals are the Finance app's display, not part of the value**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals).

**Its twin function answers without the app** (emulator):
[TvmPV](finance/TvmPV.md) was measured from a batch nobody had touched, so
with the Function app active, and it answered. A program that cannot select
the Finance app can still compute this value by calling the function with
its arguments.

**A program can set it, and the value stays** (emulator). With the Finance app
active, `PV:=1000` answered `1000.00` and a later read in the same pass
answered `1000.00`. That is the only assignment measured in this app.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmPV](finance/TvmPV.md) · [PPYR](finance/PPYR.md) · [BEG](finance/BEG.md) · [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals)

---

<a name="Payback"></a>

## Payback

The payback period, refused with or without the Finance app.

| | |
|---|---|
| Syntax | `Payback` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Payback")` | *error* | [emulator](results.tsv) |
| `EXPR(" Payback")` | *error* | [emulator](results.tsv) |

### Behaviour

**It refused with the Function app active and again with Finance's**
(emulator). So this is not the app rule, which freed nine of its neighbours
the moment Finance was selected -- see [PV](finance/PV.md). Something other than the
active app keeps it from answering.

**It holds the payback period of the cash flows** (unverified): HP's list gives this name and its app, and
nothing more.

**The likeliest reason is that there is nothing to compute from** (unverified).
[CFData](finance/CFData.md), the list of cash flows, read `{}` in the same batches,
and eight results of those cash flows -- this one among them -- refused while
[TotalCF](finance/TotalCF.md), their plain sum, answered 0. A sum of nothing is 0; a
present value, a rate or a payback period of nothing has none to give. Filling `CFData` and reading this
again would settle it.

**Its twin function answers, given the flows as arguments** (emulator):
[CashFlowPB](finance/CashFlowPB.md) was measured and returned a value. A program
wanting this number does not need the app at all.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CashFlowPB](finance/CashFlowPB.md) · [CFData](finance/CFData.md) · [TotalCF](finance/TotalCF.md)

---

<a name="PercentChange"></a>

## PercentChange

How far the new value is from the old one, as a percentage of the old.

| | |
|---|---|
| Syntax | `PercentChange(old,new)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PercentChange(100,110)")` | `10` | [emulator](results.tsv) |

### Behaviour

`PercentChange(100,110)` answers 10 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes): 110 is ten per cent
above 100.

**The answer is the change, not the total** (emulator).
[PercentTotal](finance/PercentTotal.md) takes the same two arguments and answers 110,
which is the new value as a percentage of the old. One says how much it
moved, the other says where it landed, and confusing them is off by a
hundred.

The arguments go old first, new second (HP help), and the answer is
consistent with that order: the other way round would give a negative number
near nine.

What it answers when the old value is zero was not run (unverified), and that
is the case a program has to guard, since the division has nothing to divide
by.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PercentTotal](finance/PercentTotal.md) · [PercentMargin](finance/PercentMargin.md) ·
[PercentMarkup](finance/PercentMarkup.md)

---

<a name="PercentMargin"></a>

## PercentMargin

The difference between cost and price, over the cost.

| | |
|---|---|
| Syntax | `PercentMargin(cost,price)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PercentMargin(60,100)")` | `66.6666666667` | [emulator](results.tsv) |

### Behaviour

`PercentMargin(60,100)` answers 66.6666666667 (emulator), a plain real of
type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**That is the difference over the cost, not over the price** (emulator). With
a cost of 60 and a price of 100 the difference is 40; 40 over 60 is
66.6666666667 and 40 over 100 is 40. The answer is the first of those, so
this name divides by the cost.

**Which means it does not compute what a margin usually means** (emulator).
In ordinary accounting a margin is the difference over the *price* -- 40 here
-- and what this answers is the figure usually called the markup on cost.
[PercentMarkup](finance/PercentMarkup.md) answers 40 for the same two arguments, so
the two names hold each other's usual meanings.

The arguments go in cost first, price second (HP help), and the answer above
is consistent with that order: reading them the other way would give a
negative number.

What it does when the price is below the cost was not run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Models get wrong

| They write | What happens | Seen in |
|---|---|---|
| `PercentMargin(cost,price)` for a profit margin | Answers the difference over the *cost*, which is larger than the margin and never flagged. The margin over the price is [PercentMarkup](finance/PercentMarkup.md) | [emulator](results.tsv) |

### Related

[PercentMarkup](finance/PercentMarkup.md) · [PercentChange](finance/PercentChange.md) ·
[PercentTotal](finance/PercentTotal.md)

---

<a name="PercentMarkup"></a>

## PercentMarkup

The difference between cost and price, over the price.

| | |
|---|---|
| Syntax | `PercentMarkup(cost,price)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PercentMarkup(60,100)")` | `40` | [emulator](results.tsv) |

### Behaviour

`PercentMarkup(60,100)` answers 40 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**That is the difference over the price** (emulator). With a cost of 60 and a
price of 100 the difference is 40, and 40 over 100 is 40. Dividing by the
cost instead would give 66.6666666667, which is what
[PercentMargin](finance/PercentMargin.md) answers for the same two arguments.

**Which means it does not compute what a markup usually means** (emulator).
In ordinary accounting a markup is the difference over the *cost*, and what
this answers is the figure usually called the margin. The two names on this
calculator hold each other's usual meanings.

The arguments go in cost first, price second (HP help).

What it does when the price is below the cost was not run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Models get wrong

| They write | What happens | Seen in |
|---|---|---|
| `PercentMarkup(cost,price)` for a markup on cost | Answers the difference over the *price*, which is the smaller number and is never flagged. The markup over the cost is [PercentMargin](finance/PercentMargin.md) | [emulator](results.tsv) |

### Related

[PercentMargin](finance/PercentMargin.md) · [PercentChange](finance/PercentChange.md) ·
[PercentTotal](finance/PercentTotal.md)

---

<a name="PercentTotal"></a>

## PercentTotal

The new value as a percentage of the old one.

| | |
|---|---|
| Syntax | `PercentTotal(old,new)` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PercentTotal(100,110)")` | `110` | [emulator](results.tsv) |

### Behaviour

`PercentTotal(100,110)` answers 110 (emulator), a plain real of type 0,
[ppl.type-codes](../topics/ppl.md#ppl.type-codes): 110 is a hundred and
ten per cent of 100.

**This is where the value landed, not how far it moved** (emulator).
[PercentChange](finance/PercentChange.md) answers 10 for the same two arguments. The
two differ by exactly one hundred whenever the old value is positive, which
is what makes them easy to mix up and hard to notice.

The arguments go old first, new second (HP help).

What it answers when the old value is zero was not run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PercentChange](finance/PercentChange.md) · [PercentMargin](finance/PercentMargin.md) ·
[PercentMarkup](finance/PercentMarkup.md)

---

<a name="Price"></a>

## Price

The unit price of a break-even calculation.

| | |
|---|---|
| Syntax | `Price` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Price")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the price of one unit in a break-even** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Quantity](finance/Quantity.md) · [FixedCost](finance/FixedCost.md) · [Profit](finance/Profit.md)

---

<a name="PriceBond"></a>

## PriceBond

A bond’s price.

| | |
|---|---|
| Syntax | `PriceBond` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("PriceBond")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the price of a bond, per 100 of face value** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[YieldBond](finance/YieldBond.md) · [CpnPer](finance/CpnPer.md) · [Accrued](finance/Accrued.md)

---

<a name="Profit"></a>

## Profit

The profit of a break-even calculation.

| | |
|---|---|
| Syntax | `Profit` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Profit")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the profit a break-even produces or is set to** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Price](finance/Price.md) · [Quantity](finance/Quantity.md) · [FixedCost](finance/FixedCost.md)

---

<a name="Quantity"></a>

## Quantity

The units sold in a break-even calculation.

| | |
|---|---|
| Syntax | `Quantity` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Quantity")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how many units a break-even counts** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Price](finance/Price.md) · [Profit](finance/Profit.md) · [VariableCost](finance/VariableCost.md)

---

<a name="RiskFree"></a>

## RiskFree

The risk-free rate in a Black-Scholes calculation.

| | |
|---|---|
| Syntax | `RiskFree` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("RiskFree")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the risk-free rate a Black-Scholes value discounts at** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Volatility](finance/Volatility.md) · [BSCall](finance/BSCall.md) · [Dividend](finance/Dividend.md)

---

<a name="SafeInt"></a>

## SafeInt

The safe rate a modified return uses.

| | |
|---|---|
| Syntax | `SafeInt` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("SafeInt")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the rate negative cash flows are assumed to be financed at** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[InvestInt](finance/InvestInt.md) · [FMRR](finance/FMRR.md) · [MIRR](finance/MIRR.md)

---

<a name="SalePrice"></a>

## SalePrice

The sale price of a margin or markup calculation.

| | |
|---|---|
| Syntax | `SalePrice` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("SalePrice")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the sale price a margin or markup is measured against** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Cost](finance/Cost.md) · [Margin](finance/Margin.md) · [Markup](finance/Markup.md)

---

<a name="SalvageAsset"></a>

## SalvageAsset

The salvage value of an asset being depreciated.

| | |
|---|---|
| Syntax | `SalvageAsset` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("SalvageAsset")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds what an asset is worth at the end of its life** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CostAsset](finance/CostAsset.md) · [LifeAsset](finance/LifeAsset.md) · [FactorDepr](finance/FactorDepr.md)

---

<a name="SemiAnnual"></a>

## SemiAnnual

Whether a bond pays its coupon twice a year.

| | |
|---|---|
| Syntax | `SemiAnnual` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("SemiAnnual")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds a switch between annual and semi-annual coupons** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CpnPer](finance/CpnPer.md) · [Bond360](finance/Bond360.md) · [PriceBond](finance/PriceBond.md)

---

<a name="SetDate"></a>

## SetDate

A bond’s settlement date, 2017.0101 as the app arrives.

| | |
|---|---|
| Syntax | `SetDate` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("SetDate")` | `2017.0101` | [emulator](results.tsv) |

### Behaviour

**It answered `2017.0101` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the date a bond is settled** (unverified): HP’s list gives this name and its app,
and nothing more.

**2017.0101 is a default, and all five date variables of this app arrived
holding it** (emulator). Read in the YYYY.MMDD form HP's date values take, it
is 1 January 2017, which is untested here (unverified). A program reading it
as a plain number gets 2017.0101.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[MatDate](finance/MatDate.md) · [Accrued](finance/Accrued.md) · [PriceBond](finance/PriceBond.md)

---

<a name="StockPrice"></a>

## StockPrice

The stock price in a Black-Scholes calculation.

| | |
|---|---|
| Syntax | `StockPrice` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("StockPrice")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the price of the underlying stock** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[StrikePrice](finance/StrikePrice.md) · [BSCall](finance/BSCall.md) · [Volatility](finance/Volatility.md)

---

<a name="StrikePrice"></a>

## StrikePrice

The strike price in a Black-Scholes calculation.

| | |
|---|---|
| Syntax | `StrikePrice` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("StrikePrice")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the price the option can be exercised at** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[StockPrice](finance/StockPrice.md) · [BSPut](finance/BSPut.md) · [TimeMarket](finance/TimeMarket.md)

---

<a name="TimeMarket"></a>

## TimeMarket

The time to expiry in a Black-Scholes calculation.

| | |
|---|---|
| Syntax | `TimeMarket` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("TimeMarket")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds how long the option has left to run** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Volatility](finance/Volatility.md) · [StrikePrice](finance/StrikePrice.md) · [BSCall](finance/BSCall.md)

---

<a name="Total"></a>

## Total

A total, 0 on a fresh calculator.

| | |
|---|---|
| Syntax | `Total` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Total")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds a total the percentage-change worksheet keeps** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[Change](finance/Change.md) · [OldValue](finance/OldValue.md) · [NewValue](finance/NewValue.md)

---

<a name="TotalCF"></a>

## TotalCF

The total of the cash flows, 0 while there are none.

| | |
|---|---|
| Syntax | `TotalCF` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("TotalCF")` | `0` | [emulator](results.tsv) |
| `EXPR(" TotalCF")` | `0.00` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the sum of the cash flows** (unverified): HP’s list gives this name and its app,
and nothing more.

**With the Finance app active it reads `0.00`, the same value with two decimals**
(emulator), [apps.finance-shows-two-decimals](../topics/apps.md#apps.finance-shows-two-decimals). The decimals are the app's display, not part of the
value.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[CFData](finance/CFData.md) · [NPV](finance/NPV.md) · [CFPYR](finance/CFPYR.md)

---

<a name="TvmFV"></a>

## TvmFV

The future value, from the other four.

| | |
|---|---|
| Syntax | `TvmFV(NbPmt, IPYR, PV, PMT, [PPYR], [CPYR], [BEG])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("TvmFV(12,5,-1000,0)")` | `1051.16189788` | [emulator](results.tsv) |

### Behaviour

`TvmFV(12,5,-1000,0)` answers 1051.16189788 (emulator), a plain real of type
0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**The answer says what the defaults are** (emulator). Twelve payments at five
per cent a year, starting from a thousand, come to 1051.16189788, and a
thousand times one plus five over twelve hundred, raised to the twelfth, is
1051.162. So the rate is divided by twelve: payments a year defaults to
twelve, and the twelve periods here are months rather than years.

**Money paid out is negative** (emulator). The present value was given as
-1000 and the future value came back positive. A program that makes both
positive is describing a different situation and gets a different number, not
an error.

**Five names solve one equation, and this is the one for the future value**
(emulator): [TvmPV](finance/TvmPV.md), [TvmPMT](finance/TvmPMT.md), [TvmIPYR](finance/TvmIPYR.md) and
[TvmNbPmt](finance/TvmNbPmt.md) each take the other four and answer the one they are
named for. The round trip is recorded in [TvmPV](finance/TvmPV.md), which is handed
this answer and returns the thousand it started from.

The three optional arguments are payments a year, compounds a year and
whether payments fall at the beginning (HP help). None was run (unverified).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmPV](finance/TvmPV.md) · [TvmPMT](finance/TvmPMT.md) · [TvmIPYR](finance/TvmIPYR.md) ·
[TvmNbPmt](finance/TvmNbPmt.md)

---

<a name="TvmIPYR"></a>

## TvmIPYR

The interest rate a year, from the other four.

| | |
|---|---|
| Syntax | `TvmIPYR(NbPmt, PV, PMT, FV, [PPYR], [CPYR], [BEG])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("TvmIPYR(12,-1000,0,1051.16)")` | `4.9998186967` | [emulator](results.tsv) |

### Behaviour

`TvmIPYR(12,-1000,0,1051.16)` answers 4.9998186967 (emulator), a plain real
of type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It recovers the five per cent the other commands were given** (emulator).
[TvmFV](finance/TvmFV.md) turned five per cent into 1051.16189788, and handing the
printed 1051.16 back to this command returns 4.9998186967. The gap is the two
figures dropped when that number was written down, not a disagreement between
the commands.

**The answer is a rate a year, not a rate a period** (emulator). The twelve
periods are months, as [TvmFV](finance/TvmFV.md) shows, and the answer is still about
five rather than about 0.4166.

The arguments omit the rate, which is what this one answers (HP help), so the
list is one shorter in the middle than its siblings' and the positions move.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmFV](finance/TvmFV.md) · [TvmPV](finance/TvmPV.md) · [TvmPMT](finance/TvmPMT.md) ·
[TvmNbPmt](finance/TvmNbPmt.md)

---

<a name="TvmNbPmt"></a>

## TvmNbPmt

How many payments, from the other four.

| | |
|---|---|
| Syntax | `TvmNbPmt(IPYR, PV, PMT, FV, [PPYR], [CPYR], [BEG])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("TvmNbPmt(5,-1000,0,1051.16)")` | `11.9995657754` | [emulator](results.tsv) |

### Behaviour

`TvmNbPmt(5,-1000,0,1051.16)` answers 11.9995657754 (emulator), a plain real
of type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**It recovers the twelve periods the other commands were given** (emulator),
and the shortfall is the rounding in 1051.16 rather than a disagreement:
[TvmFV](finance/TvmFV.md) answered 1051.16189788 from exactly twelve.

**It answers a fraction, and a program has to decide what to do with it**
(emulator). Twelve payments cannot be 11.9995657754 of anything real, so a
caller either rounds or treats the answer as the point where the balance is
reached part way through a period. Nothing here says which the calculator
intends.

The rate comes first in this one, because the count is what it answers
(HP help).

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmFV](finance/TvmFV.md) · [TvmPV](finance/TvmPV.md) · [TvmPMT](finance/TvmPMT.md) ·
[TvmIPYR](finance/TvmIPYR.md)

---

<a name="TvmPMT"></a>

## TvmPMT

The payment, from the other four.

| | |
|---|---|
| Syntax | `TvmPMT(NbPmt, IPYR, PV, FV, [PPYR], [CPYR], [BEG])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("TvmPMT(12,5,-1000,0)")` | `85.6074817885` | [emulator](results.tsv) |

### Behaviour

`TvmPMT(12,5,-1000,0)` answers 85.6074817885 (emulator), a plain real of type
0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**That is a thousand repaid over twelve months, not twelve years**
(emulator). Twelve payments of 85.61 come to 1027.29, which is the thousand
plus the interest on a balance that falls each month, and it agrees with the
rate being divided by twelve the way [TvmFV](finance/TvmFV.md) shows.

**The arguments are not in the same order as its siblings** (HP help). This
one takes the present value and the future value, where
[TvmFV](finance/TvmFV.md) takes the present value and the payment. Each of the five
omits the quantity it answers, so the position of a number changes with the
name and a program cannot reuse one argument list across them.

Money paid out is negative (emulator): the thousand was given as -1000 and
the payment came back positive.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmFV](finance/TvmFV.md) · [TvmPV](finance/TvmPV.md) · [TvmIPYR](finance/TvmIPYR.md) ·
[TvmNbPmt](finance/TvmNbPmt.md)

---

<a name="TvmPV"></a>

## TvmPV

The present value, from the other four.

| | |
|---|---|
| Syntax | `TvmPV(NbPmt, IPYR, PMT, FV, [PPYR], [CPYR], [BEG])` |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("TvmPV(12,5,0,1051.16)")` | `−999.998194492` | [emulator](results.tsv) |
| `EXPR("TvmPV(12,5,0,TvmFV(12,5,-1000,0))")` | `−999.999999998` | [emulator](results.tsv) |

### Behaviour

`TvmPV(12,5,0,1051.16)` answers −999.998194492 (emulator), a plain real of
type 0, [ppl.type-codes](../topics/ppl.md#ppl.type-codes).

**The second row is the check that matters** (emulator). It hands this
command exactly what [TvmFV](finance/TvmFV.md) answered, rather than a rounded copy
of it, and gets back −999.999999998: the thousand the other command started
from, to nine figures. That is the calculator agreeing with itself across two
names, which is worth more than either row alone, and it is what says the
five `Tvm` commands invert one equation rather than computing five unrelated
things.

**The difference between the two rows is the rounding, not the command**
(emulator). The first was given 1051.16, the printed value, and came back
1.8 thousandths away; the second was given the full answer and came back two
billionths away.

**The minus sign is the calculator's own, U+2212** (emulator), not the
hyphen a keyboard types:
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign). Both Result cells above
were built from the stored row rather than typed.

Money paid out is negative here as everywhere in this family (emulator): a
future value of about 1051 taken as positive makes the present value come
back negative.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[TvmFV](finance/TvmFV.md) · [TvmPMT](finance/TvmPMT.md) · [TvmIPYR](finance/TvmIPYR.md) ·
[TvmNbPmt](finance/TvmNbPmt.md) ·
[ppl.minus-sign](../topics/ppl.md#ppl.minus-sign)

---

<a name="VariableCost"></a>

## VariableCost

The variable cost per unit of a break-even calculation.

| | |
|---|---|
| Syntax | `VariableCost` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("VariableCost")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the cost each further unit adds** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[FixedCost](finance/FixedCost.md) · [Quantity](finance/Quantity.md) · [Profit](finance/Profit.md)

---

<a name="Volatility"></a>

## Volatility

The volatility in a Black-Scholes calculation.

| | |
|---|---|
| Syntax | `Volatility` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("Volatility")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the volatility of the underlying stock** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[StockPrice](finance/StockPrice.md) · [TimeMarket](finance/TimeMarket.md) · [BSCall](finance/BSCall.md)

---

<a name="YieldBond"></a>

## YieldBond

A bond’s yield.

| | |
|---|---|
| Syntax | `YieldBond` → real |
| Group | finance |
| Runs on the PC | no |

### Examples

| Call | Result | Known from |
|---|---|---|
| `EXPR("YieldBond")` | `0` | [emulator](results.tsv) |

### Behaviour

**It answered `0` with the Function app active, not its own** (emulator),
on a calculator the harness had just reset. Finance is the app where this is
common: 50 of its 68 variables answered from outside it, which is not what the
Triangle Solver's or the Function app's did,
[apps.function-needs-active-app](../topics/apps.md#apps.function-needs-active-app).

**It holds the yield of a bond** (unverified): HP’s list gives this name and its app,
and nothing more.

**Whether a program can set it is untested** (unverified). [PV](finance/PV.md) took
`PV:=1000` and kept it with the Finance app active, so a Finance variable can
be written; this one was not tried.

The interpreter does not implement it, so `hpprime run` cannot check a program
that uses it (unverified).

### Related

[PriceBond](finance/PriceBond.md) · [Macaulay](finance/Macaulay.md) · [CallPrice](finance/CallPrice.md)
